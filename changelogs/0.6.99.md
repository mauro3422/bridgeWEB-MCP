# 0.6.99 — 2026-08-15

- Summary: Adopt packaged MSSR 0.2.19, add the first MauroPrime Operational Notice Plane host slice on existing `bridgeNotices`, adopt project-context/routing precision hardening, and tighten workflow-guide recommendation precision for modular architecture work.
- Areas: operational-notices, mssr-host-adapter, skill-health, project-context-health, http-scheduler, observability, project-context-routing-precision, workflow-guide-routing, documentation
- PROJECT_CONTEXT: updated
- PROJECT_MEMORY: updated
- PROJECT_STATE: updated

## Changes

- Vendor/install `@mauroprime/mssr` `0.2.19` and consume `evaluateMssrOperationalNoticeTransition(...)` instead of cloning transition policy in Bridge.
- Added `src/operational-notices.ts` to compute bounded structural fingerprints and map portable candidates onto existing `BridgeNoticeInput` envelopes.
- Skill Health scheduler now exposes the previous persisted snapshot and emits opened/changed/escalated/deescalated/resolved transitions; stable REVIEW with unchanged structural evidence does not re-notify on the next daily capture.
- Project Context Health scheduler uses the same transition behavior; WATCH remains silent by default.
- Notice actions remain bounded suggestions (`skill_route_audit`, `project_context_audit`) and are never auto-executed or treated as authorization.
- Added focused regression coverage for initial REVIEW, stable REVIEW suppression, material evidence changes, resolution, WATCH silence, and advisory action metadata.
- Added `docs/OPERATIONAL_NOTICE_PLANE_ADAPTER.md` and updated roadmap/project authorities to record MSSR-vs-host ownership.
- Repacked/adopted the verified MSSR `0.2.19` context/routing hardening: section-scoped `maxChars`, explicit host replan core omission, and stricter failure-signal routing semantics now run in the live Bridge package rather than only in the sibling source tree.
- Hardened existing-skill coverage so Figma-specific skills require explicit Figma intent instead of generic design/code token overlap.
- Expanded `safe-modular-refactoring` activation with `modularization`, `functional core`, `imperative shell`, `ownership boundaries`, and `architecture hardening`; added a regression proving a modular architecture task selects that guide and no Figma coverage.
- Preserved Bridge's current queue/TTL/pending dedupe/automatic drain/recent-history behavior; no tool catalog expansion was required.

## Verification

- `npm run check`: PASS.
- `npm run build`: PASS.
- Focused Operational Notice / Skill Health / Project Context Health regressions: PASS.
- Source/vendor/installed `@mauroprime/mssr` `0.2.19` artifact parity: PASS.
- Authoritative `bridge_verify_all` job `bridge_verify_1786832380279_1`: PASS, including HTTP smoke, dual-era MCP, full `test:regressions`, 210 effective routing fixtures, docs/tools, watchdog, metrics and tools/list sanity.
- Live watchdog adoption/readback: PASS â€” Bridge `0.6.99`, 156 tools, tunnel `live/ready`, restart ack `ddeed520-f82c-4314-af70-ebf928f57b97`.
- Post-hardening `npm run test:regressions`: PASS (exit 0), including workflow-guide routing, packaged MSSR Context Plane/Project Context, 210 routing fixtures, Skill/Project Health, and Operational Notices.
- Second live watchdog readback after workflow-guide hardening: PASS â€” restart ack `7d1be960-dc7b-496b-a97d-6a4017fe6d64`, Bridge `0.6.99`, 156 tools, tunnel `live/ready`.
- Live GodotAtlas modular-architecture bootstrap: PASS â€” only `mssr-agent-routing` + `complex-system-design`; five architecture modules selected; Spatial/Routing excluded by intent; `safe-modular-refactoring` auto-loaded; no Figma skill coverage.
- Verification note: a mistakenly duplicated concurrent verify job failed `ENOTEMPTY` in the shared project-context sandbox; the non-racing authoritative job passed the same regression suite, so this is test-runner concurrency evidence rather than a product regression.
