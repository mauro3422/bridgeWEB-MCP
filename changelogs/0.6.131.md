# 0.6.131 — 2026-09-18

- Summary: keep Project Situation focused on historical claims whose exact sources are still current canonical repository candidates, removing false `canonical-baseline-missing` without deleting delivery history or weakening real stale-context review.
- Areas: Project Situation, context receipts, observability, host health, testing
- PROJECT_CONTEXT: reviewed-none
- PROJECT_MEMORY: reviewed-none
- PROJECT_STATE: updated

## Changes

- Project Situation still applies receipt TTL/retention rules, but before evaluating current consistency it now projects each active delivery receipt down to sources whose exact `canonicalOwner + ref` identity is present in the current authoritative project observations.
- Historical receipts remain unchanged in `.mssr/runtime/context-inbox.json`; the watcher does not acknowledge, rewrite, expire, or delete them. Portable MSSR receipt/situation semantics are unchanged.
- A retained receipt for a versioned changelog that is no longer one of the repository provider's current canonical candidates no longer creates `canonical-baseline-missing` or counts as active current context merely because its TTL has not expired.
- Current authorities such as `.mssr/PROJECT_MEMORY.md` and `.mssr/PROJECT_STATE.md` remain comparable against their delivered historical revisions. Real stale revision mismatches therefore continue to produce REVIEW and `revalidate-context-evidence`.
- Multi-source receipts are filtered source-by-source; the receipt participates in the current projection when at least one exact source remains canonical.

## Verification

- Focused `npm run check && npm run build && node scripts/test-project-situation.mjs`: PASS.
- Added regression coverage for an obsolete historical version-changelog receipt alone and together with a genuinely stale current memory receipt. The obsolete source is excluded from the current projection while the memory mismatch remains REVIEW.
- Real Bridge inbox readback against freshly built `dist`: `canonical-baseline-missing` removed, `evidenceComplete=true`, two expected stale mismatches remain for PROJECT_MEMORY/PROJECT_STATE, and `nextAction=revalidate-context-evidence`.
- Full isolated `npm run test:regressions`: PASS, exit 0. Integrated liveness completed 64/64 persisted writes with six synthetic agents, p95 concurrent readiness 2.72 ms, max 48.44 ms, zero event-loop stalls and one passive WAL checkpoint.

## Project knowledge impact

- PROJECT_CONTEXT root remains accurate; Bridge continues to own host-side projection while MSSR owns portable situation/receipt semantics.
- The indexed host-health projection now records the current-canonical-source boundary so retained audit history cannot be mistaken for an active current authority.
- PROJECT_STATE records the 0.6.131 source verification/adoption lifecycle separately from the still-live 0.6.130 runtime until controlled restart/readback completes.
