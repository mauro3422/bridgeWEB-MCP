# 0.6.92 - 2026-08-14

## Contract

- Summary: make the native Godot scene tools safe for the shared multi-editor provider and align delegated Bridge wrapper authorization with each tool's final registered safety annotations. Bridge enumerates every connected Godot editor, targets scene operations explicitly, refuses ambiguous mutations, and the post-release wrapper hotfix removes drift between `bridge_tool_schema` / `riskSummary` and `bridge_tool_query` / `bridge_tool_action`.
- Areas: Godot, tools, multi-editor targeting, editor safety, Atlas integration, tool dispatch, risk classification
- PROJECT_CONTEXT: reviewed-none
- PROJECT_MEMORY: updated
- PROJECT_STATE: updated

## Changes

- `godot_mcp_instance_list` consumes provider 0.3.7's full `instances` array instead of presenting only the default editor.
- `godot_scene_open` and `godot_scene_create` expose optional exact editor selectors: `projectPath`, `projectId`, and `editorInstanceId`.
- Target identity is forwarded through provider status, scene read, create/open, and active-scene verification so evidence cannot silently come from another connected project.
- When multiple editors are connected and no target is supplied, dedicated mutating scene operations fail closed instead of using the provider's default/first editor.
- Focused fake-provider regression coverage now exercises two connected editors, instance enumeration, ambiguous mutation rejection, and explicit targeted scene operations.
- This release composes with MauroPrime Godot MCP provider 0.3.7 and GodotAtlas's provider-owned editor-only host; it does not add UI automation or duplicate provider ownership.
- Post-release wrapper hotfix: `bridge_tool_query` and `bridge_tool_action` classify their delegated target from the final registered `annotations` instead of independent static dispatch Sets. `riskSummary` uses the same classifier, so schema, health/catalog projection, and dispatch share one effective source of truth.
- Contradictory final annotations (`readOnlyHint=true` together with `destructiveHint=true`) now fail closed. Static name Sets remain only as defaults for legacy tool annotations and no longer authorize delegated execution.
- `scripts/test-project-change-consistency.mjs` now protects the original regression and nearby safety negatives: read-only query accepted, read-only action rejected, mutable query rejected, and contradictory annotations rejected.

## Verification

- `npm run check`
- `npm run build`
- `node scripts/test-godot-tools.mjs`
- `npm run docs:tools`
- `npm run test:regressions`
- `git diff --check`
- Runtime verification after the controlled Bridge HTTP restart confirmed server `0.6.92`, 149 tools, live target fields on `godot_scene_open`, and concurrent Electronics + GodotAtlas editors. Bridge targeted `D:/Dev/GodotAtlas/`, opened A/B2/B3/B4/C/D, verified D active with `uiAutomation=false`, and a follow-up instance readback confirmed Electronics remained connected with the same editor instance id.
- Wrapper hotfix verification passed `npm run check`, isolated `npm run build`, `node scripts/test-project-change-consistency.mjs`, `node scripts/test-v060-tools.mjs`, `git diff --check`, and the full `npm run test:regressions` suite.
- To avoid activating unrelated narrated-media/workflow edits, runtime `dist` was rebuilt in a detached HEAD 0.6.92 worktree with only the corrected `src/tool-registry.ts`, byte-compared into the main runtime, then restarted through the watchdog. Live readback returned `classification=read-only` for `bridge_tool_query -> project_change_consistency`; `bridge_tool_action` rejected that read-only target and `bridge_tool_query` rejected mutable `project_context_update`.
