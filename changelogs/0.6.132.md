# 0.6.132 — 2026-09-18

- Summary: refresh three vulnerable production transitive dependencies inside the dependency ranges already declared by the MCP/AJV/Express stack, removing the current npm audit findings without adding direct dependencies, new overrides, or changing Bridge/MSSR behavior.
- Areas: dependencies, security maintenance, release integrity
- PROJECT_CONTEXT: reviewed-none
- PROJECT_MEMORY: reviewed-none
- PROJECT_STATE: updated

## Changes

- `fast-uri` resolves from `3.1.5` to `3.1.8` through `ajv@8.20.0` / `@modelcontextprotocol/sdk@1.30.0`.
- `hono` resolves from `4.13.0` to `4.13.8` through the existing MCP Node/SDK dependency graph.
- `qs` resolves from `6.15.3` to `6.16.0` through the existing Express/body-parser dependency graph.
- The root dependency ranges and the existing `@hono/node-server 2.0.11` override are unchanged; no security package was promoted to a direct Bridge dependency.
- Vendored and installed `@mauroprime/mssr` remain on `0.2.64`; MSSR 0.2.65 adoption is intentionally a separate release front.

## Verification

- `npm run check`: PASS.
- `npm run build`: PASS with package/source/dist version parity at `0.6.132`.
- Full `npm audit --json` and production-only audit: PASS with 0 vulnerabilities after the bounded transitive refresh.
- `npm ls fast-uri hono qs --all`: PASS with `fast-uri 3.1.8`, `hono 4.13.8`, and `qs 6.16.0`.
- Full isolated `npm run test:regressions`: PASS, including 64/64 observability writes, six synthetic agents, concurrent readiness p95 1.36 ms, zero event-loop stalls, and one passive WAL checkpoint.
- `TOOLS.md` was regenerated through `npm run docs:tools` for Bridge 0.6.132 / 164 tools after the generated-doc freshness gate correctly detected the prior 0.6.130 header/default.
- Release commit `0c29d0352417fc40cae798ddded6075f110068b4` was published and verified equal across local HEAD, tracking ref, and direct `ls-remote`; controlled HTTP-only adoption then produced live Bridge `0.6.132` at PID `16656` / boot `f27205e9-c4e1-4e53-ac48-da136c0e9eb2`, tunnel `live/ready`, 164 tools, and MSSR `0.2.64`. Post-adoption `npm run smoke:http` and full strict-Git `bridge_verify_all` both PASS (`failedRequired=0`).

## Project knowledge impact

- PROJECT_CONTEXT remains accurate; no architecture or ownership boundary changed.
- PROJECT_MEMORY requires no new durable decision: this is a bounded dependency-security maintenance release under the existing release-verification contract.
- PROJECT_STATE tracks the 0.6.132 source/release/adoption lifecycle while retaining MSSR 0.2.64 as the Bridge package dependency.
