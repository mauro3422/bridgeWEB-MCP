# 0.6.133 — 2026-09-18

- Summary: adopt canonical MSSR 0.2.65, harden watchdog restart-ack publication against empty/partial evidence, retire stale `STATUS_CURRENT.md` current-state duplication, and resolve the Project Context module-count WATCH through one human-reviewed semantic consolidation with identical activation selectors.
- Areas: MSSR adoption, restart lifecycle, release integrity, project-context organization, project-state documentation
- PROJECT_CONTEXT: reviewed-none
- PROJECT_MEMORY: reviewed-none
- PROJECT_STATE: updated

## Changes

- Vendored `@mauroprime/mssr 0.2.65` from the canonical `D:\Dev\mssr\mauroprime-mssr-0.2.65.tgz`; source and vendor artifact SHA-256 are `9691430389cf41c0320f31331a73475715840eb3edaea936bddd0b19475fa47c`.
- Installed MSSR 0.2.65 exposes the corrected `workshop-media-editing-continuation-fix-zoom` fixture with bounded continuation context, `completedPhases=["discovery"]`, and `contextUsed=true`; portable routing/runtime semantics otherwise remain those of 0.2.64.
- `Write-RestartAck` now writes a unique UTF-8 temporary file, validates required JSON identity fields, replaces the published ack only after validation, reads it back, rejects empty/mismatched content, and always removes the temporary file.
- `scripts/test-bridge-regressions.ps1` now executes the actual watchdog ack writer and requires a non-empty parseable acknowledgement with matching id/action and no leaked temp file; the existing BOM-read regression remains.
- `STATUS_CURRENT.md` is now a small deprecated compatibility pointer instead of a copied volatile runtime snapshot. `.mssr/PROJECT_STATE.md`, `changelogs/INDEX.md`, live `bridge_health`, and Git readback remain the appropriate authorities/evidence.
- Documentation references in `README.md`, `NEXT_CHAT_PROMPT.md`, `docs/REPOSITORY_STRUCTURE.md`, and `RESTART_FLOW.md` were aligned with those ownership boundaries.
- A full human semantic review of all 25 selectable Project Context modules found exactly one safe consolidation: `bridge-trace-owner-integrity` plus `bridge-trace-lifecycle-reconciliation`. They now share `bridge-trace-integrity-and-lifecycle`, preserving their identical activation selectors and combined prior `maxChars` budget. The manifest is reduced to 24 modules and Project Context Health now reports `ok` with zero findings/recommendations; no threshold or suppression rule was changed.
- The separately active notice-history work requested to remain out of scope was not modified by this release.

## Verification before live adoption

- `npm run check`: PASS.
- `npm run build`: PASS with package/source/dist version `0.6.133`.
- `scripts/test-bridge-regressions.ps1`: PASS, including `restart ack publish is non-empty, validated, and temp-clean`.
- `npm run test:skill-routing`: PASS with 240 canonical effective cases; audit `ok=true`, `maintenanceRequired=false`, zero structural watch/review, zero duplicate conflicts.
- Installed MSSR fixture readback confirms bounded continuation context plus `completedPhases=["discovery"]` and `contextUsed=true`.
- `npm run docs:tools` / `npm run docs:tools:check`: PASS with 164 registry tools.
- Full and production npm audits: PASS with zero vulnerabilities.
- Project Context semantic consolidation verification: `project_context_load` reports manifest `valid`, `moduleCount=24`, `coreCount=2`, health `ok`, `findings=[]`, `recommendations=[]`; `npm run test:project-context` and `npm run test:skill-routing` pass, with routing audit `ok=true` and `maintenanceRequired=false`.

## Live adoption and post-restart verification

- Source release commit `86cc6102ffd8a818266a608520d7d56dac3e3c0d` was pushed normally and read back equal from local `HEAD`, `origin/main`, and the direct remote ref.
- The prior external watchdog was found absent while Bridge HTTP `0.6.132` and the tunnel remained orphaned. Recovery was bounded to replacing the old Bridge HTTP child and starting one production watchdog from the 0.6.133 script; the existing healthy tunnel was preserved.
- Watchdog PID `9788` consumed request `e97a5144-c135-4292-936a-928f47f53cc0` and published a parseable `restart-http` acknowledgement with the same request id, proving the hardened writer is active in the reloaded watchdog.
- Live Bridge is `0.6.133`, PID `3456`, runtime boot `f9aaf3e9-e259-4049-bd12-489b8d8f11bc`; tunnel health/readiness are `live/ready`, and the runtime catalog exposes 164 tools.
- Runtime package readback confirms `@mauroprime/mssr 0.2.65`. The installed continuation fixture readback confirms bounded context, `completedPhases=["discovery"]`, `contextUsed=true`, and activation expectation for `workshop-media-editing`.
- Full post-adoption job `bridge_verify_1789749302596_1` completed `ok=true`, `code=0`, `failedRequired=0` with `strictGit=true`. Doctor, check, build, HTTP smoke, dual-era MCP, regressions, routing latency, WAL maintenance, HTTP liveness, skill routing, generated-tools freshness, watchdog/metrics reachability, tools/list sanity, and Git status all passed.
- Post-adoption liveness completed 64 writes with 0 event-loop stalls; p95 readiness was `1.31 ms` and p95 concurrent readiness `1.32 ms`. Skill routing again passed 240 canonical effective cases with `maintenanceRequired=false`.

## Project knowledge impact

- PROJECT_CONTEXT root authority was reviewed and remains accurate; the selectable `.mssr/project-context.json` manifest changed by replacing two selector-identical trace entries with one, without changing activation scope.
- PROJECT_MEMORY root authority was reviewed and remains accurate; the two existing selective memory modules were consolidated into `.mssr/knowledge/observability/trace-integrity-and-lifecycle.md` with no semantic rule removed.
- PROJECT_STATE was updated for the completed 0.6.133 live adoption plus the resolved module-count WATCH and current 24-module healthy state.
