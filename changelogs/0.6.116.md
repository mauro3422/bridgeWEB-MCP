# 0.6.116 — 2026-08-22

- Summary: Adopt MSSR 0.2.55 and close the remaining first-party maintenance context monolith for ChatGPT Web.
- Areas: MSSR package adoption, skill context, ChatGPT Web, verification
- PROJECT_CONTEXT: reviewed-none
- PROJECT_MEMORY: updated
- PROJECT_STATE: updated

## Changes

- Adopts exact vendored MSSR `0.2.55` from `vendor/mauroprime-mssr-0.2.55.tgz` (618732 bytes; SHA-256 `948195cb2d504a560f91127ab5e2f9dae6f67726d03df66a0e5dd2177e783217`).
- Keeps Bridge's existing paged context and lifecycle envelope unchanged while packaged MSSR selects a compact friction index plus only conjunctively relevant topic modules.
- Prevents a generic `warning-observed` maintenance close from materializing the former 20,718-character `friction-patterns` unit; every replacement friction unit is below 5,000 characters.
- Does not read or change SSH configuration, keys, credentials, `.env`, authentication stores or remote-node identity.

## Verification

- Vendored package size/SHA-256 parity — PASS before installation.
- Packaged MSSR first-party/routing/audit/full verification and release gate — PASS at 0.2.55.
- Bridge `npm run verify:all` and controlled live restart passed at 0.6.116. The original-trace close probe then exposed a second metadata failure (58,329 characters from selected project/messages/notices), so 0.6.117 supersedes this intermediate live build before Git publication.

## Project knowledge impact

- PROJECT_CONTEXT: reviewed; Bridge remains a host adapter and packaged MSSR still owns selection/page semantics.
- PROJECT_MEMORY: updated with the invariant that context modularity must be enforced at selected unit boundaries, not only root size.
- PROJECT_STATE: updated with package/version/test/live-adoption evidence.
