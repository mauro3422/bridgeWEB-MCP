# 0.6.104 — 2026-08-16

- Summary: Adopt packaged MSSR 0.2.24 and Operational Notice Plane gate C2d. Bridge now consumes the portable evidence-first recommendation plan layered over C2c consistency diagnosis, renders only `ready` recommendations as immediate notice actions, and preserves `deferred` actions with their dependencies/blockers as bounded evidence.
- Areas: operational-notices, consistency, recommendations, decision-support, MSSR-adoption, release-parity, testing, documentation
- PROJECT_CONTEXT: updated
- PROJECT_MEMORY: updated
- PROJECT_STATE: updated

## Changes

- Packaged `@mauroprime/mssr` 0.2.24 from verified tarball SHA-256 `e17884d7bf10d2da65747cb62be03ff265e3646f3844b5a044f9912ce9e92a17`; installed `dist/consistency-recommendation.js` is byte-identical to MSSR source dist.
- `src/mssr-consistency.ts` now consumes `evaluateMssrConsistencyDecisionSupport`. Bridge keeps host action rendering only; portable MSSR owns rank, confidence, information gain, dependencies, blockers, risk, reversibility, cost, blast radius and score breakdown.
- Immediate `bridgeNotices.actions` are derived only from C2d recommendations whose status is `ready`. `deferred` recommendations remain in notice details and cannot become immediate host actions merely because C2c listed the underlying recovery candidate.
- `src/release-consistency.ts` exposes C2c diagnosis plus C2d recommendation planning in the existing `/api/mssr/release-consistency` snapshot. No new Bridge MCP tool was added.
- Example protected sequencing: package/source current with stale dist/runtime -> `rebuild-generated-artifact` is ready while `verify-live-runtime` is deferred until generated drift is repaired. Historical receipt drift -> current evidence revalidation ranks before stale-claim review/replan.
- Recommendation remains advisory. Neither a numeric score nor a Bridge notice grants permission to build, install, restart or rewrite project truth.

## Verification

- MSSR 0.2.24 full `npm run verify` — PASS, exit 0: 16 native tools, identical native/Codex/OpenCode C2d semantics, 210 routing fixtures, zero blocking audit warnings.
- MSSR source/vendor/installed C2d file parity — PASS.
- Bridge `npm run check`, `npm run build`, focused C2d consistency adapter, Operational Notice Plane and Runtime Health tests — PASS.
- Pre-adoption executable parity: package/source/dist=`0.6.104`, declared/installed MSSR=`0.2.24` — PASS.
- Live adoption/closure — PASS: one coordinated watchdog HTTP restart (`ack ffb90a23-6fca-4e76-b523-3c1c568e4dcb`) adopted Bridge 0.6.104 / MSSR 0.2.24. Live PID/boot=`17740` / `8f7fb266-85e0-45a0-9698-354c90e3edba`, tunnel `live/ready`, 156 tools. `/api/mssr/release-consistency` reports level=ok, evidenceComplete=true, zero mismatches, `recommendationPolicy=evidence-first-v1`, `recommendationMode=none`, `nextAction=null`, package/source/dist/runtime=`0.6.104`, and declared/installed MSSR=`0.2.24`. Full Bridge regressions passed before adoption and again in one clean post-adoption run (exit 0, ~108 s) with no subsequent restart. Generated-tool docs, `git diff --check`, and `project_change_consistency` are publishReady.

## Project knowledge impact

- PROJECT_CONTEXT: updated
- PROJECT_MEMORY: updated
- PROJECT_STATE: updated
