# extract-state-and-interactions

## Goal

Separar estado, routing, render y wiring sin alterar eventos ni timing.

## Instructions

Extraer un subsistema por vez. Mantener ids, clases, HTML renderizado, listeners, orden de inicialización, history/hash, foco, scroll, teclado y touch. Usar adaptadores temporales cuando ayuden a conservar compatibilidad.
