# extract-pure-modules

## Goal

Extraer primero lógica pura y compartida con el mínimo cambio observable.

## Instructions

Mover formatters, labels, escaping, sorting, selectors y view-models a módulos sin efectos laterales. Mantener firmas y orden de evaluación. Añadir pruebas unitarias o contract tests. Verificar después de cada lote.
