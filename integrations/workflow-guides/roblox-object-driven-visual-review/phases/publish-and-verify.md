# publish-and-verify

## Goal

Publish artifact and global dashboards with no missing or mismatched files.

## Instructions

Do not run strict global verification before all planned sibling runs exist, or mark unstarted runs as pending rather than missing. Verify hashes, manifest counts, zero broken links, and capture status before claiming completion.
