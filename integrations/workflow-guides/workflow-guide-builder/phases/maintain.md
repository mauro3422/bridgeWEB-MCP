# maintain

## Goal

Keep guides accurate as tools, project structure, and user preferences change.

## Instructions

Update activation triggers when recommendation produces false positives or false negatives. Update recommended tools when schemas or tool names change. Keep project-specific paths out of global guides. Preserve approved behavior while refining wording. Re-run recommendation and load validation after every change. Version guide changes in Git with the bridge code or project repository that owns them.
