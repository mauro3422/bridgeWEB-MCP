# normalize-and-stage

## Goal

Create a persistent isolated review workstation with canonical naming, semantic attributes, anchored display geometry, safe query/collision settings, and consistent lighting/scale.

## Instructions

Normalize obvious generator color drift through model attributes before evaluation. Create hero and state/growth variants as separate review displays. Keep review-only geometry outside gameplay authority.
