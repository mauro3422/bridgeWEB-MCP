# automatic-review-plan

## Goal

Derive deterministic cameras, turnarounds, state strips, animation keyframes, and detail shots from the selected object bounds and metadata.

## Instructions

Prefer object-driven camera generation over hand-authored coordinates. Hide or gate world-space labels and unrelated presentation before capture. Warm the renderer, retry failed frames, and write progress incrementally.
