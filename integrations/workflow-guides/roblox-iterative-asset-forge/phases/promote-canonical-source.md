# promote-canonical-source

## Goal

Promote only a reviewed candidate into a reusable factory or canonical model source.

## Instructions

Do not make the ProceduralModel display itself authoritative gameplay state. Extract stable representation hooks, keep logical ports/hitboxes separate, stamp a visual revision, regenerate fresh samples, playtest, save, back up, and verify hashes.
