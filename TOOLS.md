# bridge-mcp TOOLS

Generated from the modular registry definitions/schemas. Do not edit this file by hand; regenerate it with `npm run docs:tools` after schema changes.

Server: `bridge-mcp v0.6.6`

Total tools: 108

Modules loaded:

- `core`
- `file-navigation`
- `file-writing`
- `workflow-guides`
- `skill-catalog-and-roblox-proxy`
- `roblox-studio-ops`
- `binary-files`
- `images`
- `process`
- `git`
- `project`
- `workspace`
- `cache`
- `bridge-ops`
- `metrics`
- `code-intelligence`
- `code-graph`
- `python-analysis`
- `blender`
- `tablet-whiteboard`
- `bridge-workflow`
- `tool-dispatch`

Source of truth:

```txt
src/tool-registry.ts
src/tools/*.ts
BridgeToolSchema.inputSchema definitions
```

## Uso recomendado

Preferir tools explicitas antes que `run_command`. Usar `run_command` como fallback cuando no exista una tool especifica o para validaciones de shell.

Para cambios de codigo: inspeccionar con lectura/busqueda/analisis, editar con `apply_patch` o `edit_lines`, correr `bridge_verify_all`, y recien despues commitear/pushear.

## Grupos

### Core / navegacion

#### `system_info`

Return basic OS, Node, CPU, memory, hostname and cwd information.

Required: ninguno

Parametros:

- sin parametros

<details>
<summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {},
  "additionalProperties": false
}
```

</details>

#### `list_dir`

List a directory recursively with bounded depth and entry limits.

Required: path

Parametros:

- `path`: string
- `depth`: number default=1 min=1 max=5

<details>
<summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "path": {
      "type": "string"
    },
    "depth": {
      "type": "number",
      "default": 1,
      "minimum": 1,
      "maximum": 5
    }
  },
  "required": [
    "path"
  ],
  "additionalProperties": false
}
```

</details>

#### `read_text_file`

Read a UTF-8 text file with a maximum byte limit.

Required: path

Parametros:

- `path`: string
- `maxBytes`: number default=2097152

<details>
<summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "path": {
      "type": "string"
    },
    "maxBytes": {
      "type": "number",
      "default": 2097152
    }
  },
  "required": [
    "path"
  ],
  "additionalProperties": false
}
```

</details>

#### `read_file_lines`

Read a text file as numbered lines with pagination. Prefer this over reading whole code files.

Required: path

Parametros:

- `path`: string
- `startLine`: number default=1 min=1
- `endLine`: number min=1
- `maxLines`: number default=250 min=1 max=500

<details>
<summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "path": {
      "type": "string"
    },
    "startLine": {
      "type": "number",
      "default": 1,
      "minimum": 1
    },
    "endLine": {
      "type": "number",
      "minimum": 1
    },
    "maxLines": {
      "type": "number",
      "default": 250,
      "minimum": 1,
      "maximum": 500
    }
  },
  "required": [
    "path"
  ],
  "additionalProperties": false
}
```

</details>

#### `read_many_files`

Read up to 10 files or file ranges in one call, using specs like 'src/config.ts:1-80'.

Required: files

Parametros:

- `files`: array
- `maxLinesPerFile`: number default=250 min=1 max=500

<details>
<summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "files": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "minItems": 1,
      "maxItems": 10
    },
    "maxLinesPerFile": {
      "type": "number",
      "default": 250,
      "minimum": 1,
      "maximum": 500
    }
  },
  "required": [
    "files"
  ],
  "additionalProperties": false
}
```

</details>

#### `list_files_smart`

List files with language, line counts, and lightweight symbols so code structure is visible without opening every file.

Required: path

Parametros:

- `path`: string
- `depth`: number default=1 min=0 max=3
- `pattern`: string
- `showImports`: boolean default=false

<details>
<summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "path": {
      "type": "string"
    },
    "depth": {
      "type": "number",
      "default": 1,
      "minimum": 0,
      "maximum": 3
    },
    "pattern": {
      "type": "string"
    },
    "showImports": {
      "type": "boolean",
      "default": false
    }
  },
  "required": [
    "path"
  ],
  "additionalProperties": false
}
```

</details>

#### `search_files`

Search literal text across files with line numbers, nearby context, and lightweight containing function/class hints.

Required: path, pattern

Parametros:

- `path`: string
- `pattern`: string
- `filePattern`: string
- `contextLines`: number default=2 min=0 max=10
- `maxResults`: number default=50 min=1 max=200
- `caseSensitive`: boolean default=false

<details>
<summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "path": {
      "type": "string"
    },
    "pattern": {
      "type": "string"
    },
    "filePattern": {
      "type": "string"
    },
    "contextLines": {
      "type": "number",
      "default": 2,
      "minimum": 0,
      "maximum": 10
    },
    "maxResults": {
      "type": "number",
      "default": 50,
      "minimum": 1,
      "maximum": 200
    },
    "caseSensitive": {
      "type": "boolean",
      "default": false
    }
  },
  "required": [
    "path",
    "pattern"
  ],
  "additionalProperties": false
}
```

</details>

### Escritura segura

#### `write_text_file`

Write or append a UTF-8 text file, creating parent directories and verifying the final bytes/hash.

Required: path, content

Parametros:

- `path`: string
- `content`: string
- `append`: boolean default=false

<details>
<summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "path": {
      "type": "string"
    },
    "content": {
      "type": "string"
    },
    "append": {
      "type": "boolean",
      "default": false
    }
  },
  "required": [
    "path",
    "content"
  ],
  "additionalProperties": false
}
```

</details>

#### `apply_patch`

Exact string replacement patch for one text file with replacement count and postflight hash verification.

Required: path, oldText, newText

Parametros:

- `path`: string
- `oldText`: string
- `newText`: string
- `expectedReplacements`: number default=1

<details>
<summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "path": {
      "type": "string"
    },
    "oldText": {
      "type": "string"
    },
    "newText": {
      "type": "string"
    },
    "expectedReplacements": {
      "type": "number",
      "default": 1
    }
  },
  "required": [
    "path",
    "oldText",
    "newText"
  ],
  "additionalProperties": false
}
```

</details>

#### `edit_lines`

Surgically edit a text file by line numbers. Supports replace, insert_before, insert_after, and delete with context and postflight verification.

Required: path, startLine

Parametros:

- `path`: string
- `startLine`: number min=1
- `endLine`: number min=1
- `newContent`: string
- `mode`: string enum=replace|insert_before|insert_after|delete
- `previewContext`: number default=2 min=0 max=10

<details>
<summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "path": {
      "type": "string"
    },
    "startLine": {
      "type": "number",
      "minimum": 1
    },
    "endLine": {
      "type": "number",
      "minimum": 1
    },
    "newContent": {
      "type": "string"
    },
    "mode": {
      "type": "string",
      "enum": [
        "replace",
        "insert_before",
        "insert_after",
        "delete"
      ]
    },
    "previewContext": {
      "type": "number",
      "default": 2,
      "minimum": 0,
      "maximum": 10
    }
  },
  "required": [
    "path",
    "startLine"
  ],
  "additionalProperties": false
}
```

</details>

### Archivos binarios

#### `binary_file_info`

Inspect an allowed binary file without printing its contents. Returns byte size, SHA-256, detected MIME type, and modification time.

Required: path

Parametros:

- `path`: string

<details>
<summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "path": {
      "type": "string"
    }
  },
  "required": [
    "path"
  ],
  "additionalProperties": false
}
```

</details>

#### `binary_file_read_chunk`

Read a bounded byte range from an allowed binary file and return it as base64, base64url, or hex. Use offsets to transfer large files safely.

Required: path

Parametros:

- `path`: string
- `offset`: number default=0 min=0
- `maxBytes`: number default=32768 min=1 max=262144
- `encoding`: string enum=base64|base64url|hex default="base64url"

<details>
<summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "path": {
      "type": "string"
    },
    "offset": {
      "type": "number",
      "default": 0,
      "minimum": 0
    },
    "maxBytes": {
      "type": "number",
      "default": 32768,
      "minimum": 1,
      "maximum": 262144
    },
    "encoding": {
      "type": "string",
      "enum": [
        "base64",
        "base64url",
        "hex"
      ],
      "default": "base64url"
    }
  },
  "required": [
    "path"
  ],
  "additionalProperties": false
}
```

</details>

#### `binary_file_write`

Write a small binary payload atomically from base64, base64url, or hex. Validates optional expected byte count and SHA-256 before replacing the target.

Required: outputPath, data

Parametros:

- `outputPath`: string
- `data`: string
- `encoding`: string enum=base64|base64url|hex default="base64"
- `overwrite`: boolean default=false
- `expectedBytes`: number min=0 max=67108864
- `expectedSha256`: string

<details>
<summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "outputPath": {
      "type": "string"
    },
    "data": {
      "type": "string",
      "maxLength": 524288
    },
    "encoding": {
      "type": "string",
      "enum": [
        "base64",
        "base64url",
        "hex"
      ],
      "default": "base64"
    },
    "overwrite": {
      "type": "boolean",
      "default": false
    },
    "expectedBytes": {
      "type": "number",
      "minimum": 0,
      "maximum": 67108864
    },
    "expectedSha256": {
      "type": "string",
      "pattern": "^[0-9a-fA-F]{64}$"
    }
  },
  "required": [
    "outputPath",
    "data"
  ],
  "additionalProperties": false
}
```

</details>

#### `binary_upload_begin`

Begin a resumable encoded binary upload for a large file. Returns an uploadId and next sequence number; append bounded chunks, then finish atomically.

Required: outputPath

Parametros:

- `outputPath`: string
- `encoding`: string enum=base64|base64url|hex default="base64"
- `overwrite`: boolean default=false
- `expectedBytes`: number min=0 max=67108864
- `expectedSha256`: string

<details>
<summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "outputPath": {
      "type": "string"
    },
    "encoding": {
      "type": "string",
      "enum": [
        "base64",
        "base64url",
        "hex"
      ],
      "default": "base64"
    },
    "overwrite": {
      "type": "boolean",
      "default": false
    },
    "expectedBytes": {
      "type": "number",
      "minimum": 0,
      "maximum": 67108864
    },
    "expectedSha256": {
      "type": "string",
      "pattern": "^[0-9a-fA-F]{64}$"
    }
  },
  "required": [
    "outputPath"
  ],
  "additionalProperties": false
}
```

</details>

#### `binary_upload_append`

Append one bounded encoded chunk to a resumable binary upload. Sequence numbers are enforced so interrupted transfers can resume safely.

Required: uploadId, sequence, chunk

Parametros:

- `uploadId`: string
- `sequence`: number min=0
- `chunk`: string

<details>
<summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "uploadId": {
      "type": "string"
    },
    "sequence": {
      "type": "number",
      "minimum": 0
    },
    "chunk": {
      "type": "string",
      "maxLength": 24576
    }
  },
  "required": [
    "uploadId",
    "sequence",
    "chunk"
  ],
  "additionalProperties": false
}
```

</details>

#### `binary_upload_status`

Inspect a resumable binary upload and return its target, encoding, accumulated characters, next sequence, expectations, and expiry.

Required: uploadId

Parametros:

- `uploadId`: string

<details>
<summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "uploadId": {
      "type": "string"
    }
  },
  "required": [
    "uploadId"
  ],
  "additionalProperties": false
}
```

</details>

#### `binary_upload_finish`

Decode and atomically commit a resumable binary upload after validating size and SHA-256. Successful uploads remove their temporary session.

Required: uploadId

Parametros:

- `uploadId`: string

<details>
<summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "uploadId": {
      "type": "string"
    }
  },
  "required": [
    "uploadId"
  ],
  "additionalProperties": false
}
```

</details>

#### `binary_upload_abort`

Delete a resumable binary upload session without touching its target file.

Required: uploadId

Parametros:

- `uploadId`: string

<details>
<summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "uploadId": {
      "type": "string"
    }
  },
  "required": [
    "uploadId"
  ],
  "additionalProperties": false
}
```

</details>

### Guias reutilizables

#### `project_context_load`

Use this once when beginning substantial work in a known repository, resuming a project, or when project-specific rules may affect the task. Loads the root AGENTS file, .bridge project context/state documents, available workflow guides, and an optional guide recommendation for the current task.

Required: projectRoot

Parametros:

- `projectRoot`: string
- `task`: string
- `includeAgents`: boolean default=true
- `includeProjectContext`: boolean default=true
- `includeGuides`: boolean default=true

<details>
<summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "projectRoot": {
      "type": "string",
      "description": "Repository or project root."
    },
    "task": {
      "type": "string",
      "description": "Optional current user request used to recommend a workflow guide."
    },
    "includeAgents": {
      "type": "boolean",
      "default": true
    },
    "includeProjectContext": {
      "type": "boolean",
      "default": true
    },
    "includeGuides": {
      "type": "boolean",
      "default": true
    }
  },
  "required": [
    "projectRoot"
  ],
  "additionalProperties": false
}
```

</details>

#### `workflow_guide_recommend`

Use this when a user describes a repeatable multi-step process, says it should happen every time or in future, asks for a skill/pipeline/template/hook, or when an existing reusable workflow may apply. Searches project and global guides, scores activation patterns, and recommends loading an existing guide or proposing a new one.

Required: task

Parametros:

- `task`: string
- `projectRoot`: string
- `maxResults`: number default=5 min=1 max=20

<details>
<summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "task": {
      "type": "string",
      "description": "The user's current request or workflow description."
    },
    "projectRoot": {
      "type": "string",
      "description": "Optional project root; project guides override global guides."
    },
    "maxResults": {
      "type": "number",
      "default": 5,
      "minimum": 1,
      "maximum": 20
    }
  },
  "required": [
    "task"
  ],
  "additionalProperties": false
}
```

</details>

#### `workflow_guide_load`

Use this after workflow_guide_recommend selects a guide, or when the user explicitly names one. Loads the versioned guide prompt, an optional phase document, recommended tools, and optional project context so ChatGPT can follow it as active instructions.

Required: name

Parametros:

- `name`: string
- `phase`: string
- `projectRoot`: string
- `projectContextPath`: string
- `includeManifest`: boolean default=true

<details>
<summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "Guide slug."
    },
    "phase": {
      "type": "string",
      "description": "Optional phase key from the guide manifest."
    },
    "projectRoot": {
      "type": "string",
      "description": "Optional project root used to discover project-specific guides first."
    },
    "projectContextPath": {
      "type": "string",
      "description": "Optional allowed file containing project-specific brief or configuration."
    },
    "includeManifest": {
      "type": "boolean",
      "default": true
    }
  },
  "required": [
    "name"
  ],
  "additionalProperties": false
}
```

</details>

#### `workflow_guide_create`

Use this only when the user asks to create/save a reusable skill-like workflow guide, or explicitly approves a recommendation to do so. Scaffolds a versioned global Bridge MCP guide or project-specific guide with activation triggers, phases, recommended tools, and resumable verification instructions.

Required: name, title, description

Parametros:

- `scope`: string enum=global|project default="project"
- `projectRoot`: string
- `name`: string
- `title`: string
- `description`: string
- `keywords`: array default=[]
- `triggerPhrases`: array default=[]
- `negativeKeywords`: array default=[]
- `examples`: array default=[]
- `phases`: array default=[]
- `recommendedTools`: array default=[]
- `overwrite`: boolean default=false

<details>
<summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "scope": {
      "type": "string",
      "enum": [
        "global",
        "project"
      ],
      "default": "project"
    },
    "projectRoot": {
      "type": "string"
    },
    "name": {
      "type": "string",
      "pattern": "^[a-z0-9]+(?:-[a-z0-9]+)*$"
    },
    "title": {
      "type": "string"
    },
    "description": {
      "type": "string"
    },
    "keywords": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "default": []
    },
    "triggerPhrases": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "default": []
    },
    "negativeKeywords": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "default": []
    },
    "examples": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "default": []
    },
    "phases": {
      "type": "array",
      "maxItems": 24,
      "items": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "pattern": "^[a-z0-9]+(?:-[a-z0-9]+)*$"
          },
          "goal": {
            "type": "string"
          },
          "instructions": {
            "type": "string"
          }
        },
        "required": [
          "name",
          "goal"
        ],
        "additionalProperties": false
      },
      "default": []
    },
    "recommendedTools": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "default": []
    },
    "overwrite": {
      "type": "boolean",
      "default": false
    }
  },
  "required": [
    "name",
    "title",
    "description"
  ],
  "additionalProperties": false
}
```

</details>

### Imagenes

#### `image_asset_save`

Use this when ChatGPT has generated or edited one or more images that must be persisted on MauroPrime. Saves one image or an atomic batch from base64/data URLs, validates signatures, records hashes and dimensions, and can write a JSON manifest.

Required: items

Parametros:

- `items`: array
- `overwrite`: boolean default=false
- `manifestPath`: string
- `collectionName`: string

<details>
<summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "items": {
      "type": "array",
      "minItems": 1,
      "maxItems": 8,
      "items": {
        "type": "object",
        "properties": {
          "outputPath": {
            "type": "string"
          },
          "base64": {
            "type": "string",
            "maxLength": 15728640
          },
          "role": {
            "type": "string"
          },
          "prompt": {
            "type": "string"
          },
          "source": {
            "type": "string"
          },
          "metadata": {
            "type": "object",
            "additionalProperties": true
          }
        },
        "required": [
          "outputPath",
          "base64"
        ],
        "additionalProperties": false
      }
    },
    "overwrite": {
      "type": "boolean",
      "default": false
    },
    "manifestPath": {
      "type": "string"
    },
    "collectionName": {
      "type": "string"
    }
  },
  "required": [
    "items"
  ],
  "additionalProperties": false
}
```

</details>

#### `image_character_views_prepare`

Use this after image_asset_save when a character has exactly one front, side, back, and three-quarter source view. Normalizes the set for Blender, aligns feet and scale, exports lightweight references, and returns quality warnings plus a manifest.

Required: baseName, items, outputDir

Parametros:

- `baseName`: string
- `items`: array
- `outputDir`: string
- `manifestPath`: string
- `targetWidth`: number default=1024 min=256 max=4096
- `targetHeight`: number default=1280 min=256 max=4096
- `backgroundThreshold`: number default=10 min=1 max=80
- `cropMargin`: number default=0.04 min=0 max=0.3
- `canvasMargin`: number default=0.06 min=0 max=0.3
- `outputFormat`: string enum=jpeg|png default="jpeg"
- `jpegQuality`: number default=92 min=50 max=100
- `overwrite`: boolean default=false
- `timeoutMs`: number default=180000 min=1000 max=600000

<details>
<summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "baseName": {
      "type": "string"
    },
    "items": {
      "type": "array",
      "minItems": 4,
      "maxItems": 4,
      "items": {
        "type": "object",
        "properties": {
          "role": {
            "type": "string",
            "enum": [
              "front",
              "side",
              "back",
              "three-quarter"
            ]
          },
          "inputPath": {
            "type": "string"
          }
        },
        "required": [
          "role",
          "inputPath"
        ],
        "additionalProperties": false
      }
    },
    "outputDir": {
      "type": "string"
    },
    "manifestPath": {
      "type": "string"
    },
    "targetWidth": {
      "type": "number",
      "default": 1024,
      "minimum": 256,
      "maximum": 4096
    },
    "targetHeight": {
      "type": "number",
      "default": 1280,
      "minimum": 256,
      "maximum": 4096
    },
    "backgroundThreshold": {
      "type": "number",
      "default": 10,
      "minimum": 1,
      "maximum": 80
    },
    "cropMargin": {
      "type": "number",
      "default": 0.04,
      "minimum": 0,
      "maximum": 0.3
    },
    "canvasMargin": {
      "type": "number",
      "default": 0.06,
      "minimum": 0,
      "maximum": 0.3
    },
    "outputFormat": {
      "type": "string",
      "enum": [
        "jpeg",
        "png"
      ],
      "default": "jpeg"
    },
    "jpegQuality": {
      "type": "number",
      "default": 92,
      "minimum": 50,
      "maximum": 100
    },
    "overwrite": {
      "type": "boolean",
      "default": false
    },
    "timeoutMs": {
      "type": "number",
      "default": 180000,
      "minimum": 1000,
      "maximum": 600000
    }
  },
  "required": [
    "baseName",
    "items",
    "outputDir"
  ],
  "additionalProperties": false
}
```

</details>

### Ejecucion / terminal

#### `run_command`

Run a shell command in a cwd with timeout and captured stdout/stderr.

Required: command

Parametros:

- `command`: string
- `cwd`: string
- `timeoutMs`: number default=30000

<details>
<summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "command": {
      "type": "string"
    },
    "cwd": {
      "type": "string"
    },
    "timeoutMs": {
      "type": "number",
      "default": 30000
    }
  },
  "required": [
    "command"
  ],
  "additionalProperties": false
}
```

</details>

#### `terminal_start`

Start a persistent terminal process and return a session id. Supports optional name, logFile, timeoutMs, and cleanupAfterMs for long-running tasks.

Required: ninguno

Parametros:

- `command`: string
- `cwd`: string
- `name`: string
- `logFile`: string
- `timeoutMs`: number min=1000 max=86400000
- `cleanupAfterMs`: number default=600000 min=0 max=86400000

<details>
<summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "command": {
      "type": "string"
    },
    "cwd": {
      "type": "string"
    },
    "name": {
      "type": "string"
    },
    "logFile": {
      "type": "string"
    },
    "timeoutMs": {
      "type": "number",
      "minimum": 1000,
      "maximum": 86400000
    },
    "cleanupAfterMs": {
      "type": "number",
      "default": 600000,
      "minimum": 0,
      "maximum": 86400000
    }
  },
  "additionalProperties": false
}
```

</details>

#### `terminal_write`

Write input to a persistent terminal session.

Required: sessionId, input

Parametros:

- `sessionId`: string
- `input`: string

<details>
<summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "sessionId": {
      "type": "string"
    },
    "input": {
      "type": "string"
    }
  },
  "required": [
    "sessionId",
    "input"
  ],
  "additionalProperties": false
}
```

</details>

#### `terminal_read`

Read buffered stdout/stderr from a persistent terminal session.

Required: sessionId

Parametros:

- `sessionId`: string
- `maxChars`: number default=20000

<details>
<summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "sessionId": {
      "type": "string"
    },
    "maxChars": {
      "type": "number",
      "default": 20000
    }
  },
  "required": [
    "sessionId"
  ],
  "additionalProperties": false
}
```

</details>

#### `terminal_stop`

Stop and forget a persistent terminal session.

Required: sessionId

Parametros:

- `sessionId`: string

<details>
<summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "sessionId": {
      "type": "string"
    }
  },
  "required": [
    "sessionId"
  ],
  "additionalProperties": false
}
```

</details>

#### `terminal_list`

List active persistent terminal sessions.

Required: ninguno

Parametros:

- sin parametros

<details>
<summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {},
  "additionalProperties": false
}
```

</details>

### Blender

#### `blender_status`

Check the configured Blender installation and whether the local interactive Blender bridge is connected.

Required: ninguno

Parametros:

- `port`: number default=9877 min=1024 max=65535

<details>
<summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "port": {
      "type": "number",
      "default": 9877,
      "minimum": 1024,
      "maximum": 65535
    }
  },
  "additionalProperties": false
}
```

</details>

#### `blender_open`

Launch Blender with the Mauro local bridge enabled. Optionally open a .blend file inside allowed roots.

Required: ninguno

Parametros:

- `blendFile`: string
- `port`: number default=9877 min=1024 max=65535

<details>
<summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "blendFile": {
      "type": "string"
    },
    "port": {
      "type": "number",
      "default": 9877,
      "minimum": 1024,
      "maximum": 65535
    }
  },
  "additionalProperties": false
}
```

</details>

#### `blender_scene_info`

Inspect objects, mesh counts, armatures, actions and totals from the connected Blender scene.

Required: ninguno

Parametros:

- `objectLimit`: number default=100 min=1 max=1000
- `port`: number default=9877 min=1024 max=65535

<details>
<summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "objectLimit": {
      "type": "number",
      "default": 100,
      "minimum": 1,
      "maximum": 1000
    },
    "port": {
      "type": "number",
      "default": 9877,
      "minimum": 1024,
      "maximum": 65535
    }
  },
  "additionalProperties": false
}
```

</details>

#### `blender_viewport_screenshot`

Capture the connected Blender 3D viewport to a PNG path inside allowed roots.

Required: outputPath

Parametros:

- `outputPath`: string
- `maxSize`: number default=1200 min=200 max=4096
- `port`: number default=9877 min=1024 max=65535

<details>
<summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "outputPath": {
      "type": "string"
    },
    "maxSize": {
      "type": "number",
      "default": 1200,
      "minimum": 200,
      "maximum": 4096
    },
    "port": {
      "type": "number",
      "default": 9877,
      "minimum": 1024,
      "maximum": 65535
    }
  },
  "required": [
    "outputPath"
  ],
  "additionalProperties": false
}
```

</details>

#### `blender_review_bundle`

Create a multi-view Blender review package in one call: orthographic renders, a contact sheet, and a manifest with geometry, materials, visibility, collections, rig, animation, diagnostics, hashes, and restored-scene confirmation.

Required: outputDir

Parametros:

- `outputDir`: string
- `filePrefix`: string
- `views`: array default=["front","right","back","three-quarter"]
- `targetCollections`: array default=[]
- `targetObjects`: array default=[]
- `resolution`: number default=800 min=320 max=2048
- `margin`: number default=1.18 min=1.01 max=2
- `transparentBackground`: boolean default=false
- `createContactSheet`: boolean default=true
- `includePreview`: boolean default=true
- `overwrite`: boolean default=false
- `port`: number default=9877 min=1024 max=65535
- `timeoutMs`: number default=300000 min=1000 max=600000

<details>
<summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "outputDir": {
      "type": "string",
      "description": "Allowed directory where review images and the JSON manifest will be written."
    },
    "filePrefix": {
      "type": "string",
      "description": "Optional stable prefix. When omitted, a timestamped prefix is generated."
    },
    "views": {
      "type": "array",
      "items": {
        "type": "string",
        "enum": [
          "front",
          "right",
          "back",
          "left",
          "three-quarter",
          "three-quarter-left",
          "rear-three-quarter",
          "top"
        ]
      },
      "default": [
        "front",
        "right",
        "back",
        "three-quarter"
      ],
      "minItems": 1,
      "maxItems": 8
    },
    "targetCollections": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "default": [],
      "maxItems": 20
    },
    "targetObjects": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "default": [],
      "maxItems": 200
    },
    "resolution": {
      "type": "number",
      "default": 800,
      "minimum": 320,
      "maximum": 2048
    },
    "margin": {
      "type": "number",
      "default": 1.18,
      "minimum": 1.01,
      "maximum": 2
    },
    "transparentBackground": {
      "type": "boolean",
      "default": false
    },
    "createContactSheet": {
      "type": "boolean",
      "default": true
    },
    "includePreview": {
      "type": "boolean",
      "default": true
    },
    "overwrite": {
      "type": "boolean",
      "default": false
    },
    "port": {
      "type": "number",
      "default": 9877,
      "minimum": 1024,
      "maximum": 65535
    },
    "timeoutMs": {
      "type": "number",
      "default": 300000,
      "minimum": 1000,
      "maximum": 600000
    }
  },
  "required": [
    "outputDir"
  ],
  "additionalProperties": false
}
```

</details>

#### `blender_execute_code`

Execute bounded Python code in the connected Blender main thread and return stdout plus an optional result variable.

Required: code

Parametros:

- `code`: string
- `port`: number default=9877 min=1024 max=65535
- `timeoutMs`: number default=180000 min=1000 max=600000

<details>
<summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "code": {
      "type": "string",
      "maxLength": 100000
    },
    "port": {
      "type": "number",
      "default": 9877,
      "minimum": 1024,
      "maximum": 65535
    },
    "timeoutMs": {
      "type": "number",
      "default": 180000,
      "minimum": 1000,
      "maximum": 600000
    }
  },
  "required": [
    "code"
  ],
  "additionalProperties": false
}
```

</details>

#### `blender_batch_script`

Run an allowed Python script through Blender in background mode, optionally against an allowed .blend file.

Required: scriptPath

Parametros:

- `scriptPath`: string
- `blendFile`: string
- `scriptArgs`: array default=[]
- `cwd`: string
- `timeoutMs`: number default=180000 min=1000 max=600000

<details>
<summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "scriptPath": {
      "type": "string"
    },
    "blendFile": {
      "type": "string"
    },
    "scriptArgs": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "default": []
    },
    "cwd": {
      "type": "string"
    },
    "timeoutMs": {
      "type": "number",
      "default": 180000,
      "minimum": 1000,
      "maximum": 600000
    }
  },
  "required": [
    "scriptPath"
  ],
  "additionalProperties": false
}
```

</details>

#### `blender_store_reference_image`

Store a generated PNG/JPEG/WebP reference image from a base64 or data-URL payload into an allowed project path.

Required: outputPath, base64

Parametros:

- `outputPath`: string
- `base64`: string
- `overwrite`: boolean default=false

<details>
<summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "outputPath": {
      "type": "string"
    },
    "base64": {
      "type": "string",
      "maxLength": 12582912
    },
    "overwrite": {
      "type": "boolean",
      "default": false
    }
  },
  "required": [
    "outputPath",
    "base64"
  ],
  "additionalProperties": false
}
```

</details>

#### `blender_setup_character_references`

Atomically validate four character views, create a resumable Blender reference scene, save its manifest, open Blender, and verify the local connection.

Required: characterName, frontImage, sideImage, backImage, threeQuarterImage, outputBlend

Parametros:

- `characterName`: string
- `frontImage`: string
- `sideImage`: string
- `backImage`: string
- `threeQuarterImage`: string
- `outputBlend`: string
- `height`: number default=6 min=0.5 max=50
- `opacity`: number default=0.55 min=0.05 max=1
- `overwrite`: boolean default=false
- `openAfter`: boolean default=true
- `port`: number default=9877 min=1024 max=65515

<details>
<summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "characterName": {
      "type": "string"
    },
    "frontImage": {
      "type": "string"
    },
    "sideImage": {
      "type": "string"
    },
    "backImage": {
      "type": "string"
    },
    "threeQuarterImage": {
      "type": "string"
    },
    "outputBlend": {
      "type": "string"
    },
    "height": {
      "type": "number",
      "default": 6,
      "minimum": 0.5,
      "maximum": 50
    },
    "opacity": {
      "type": "number",
      "default": 0.55,
      "minimum": 0.05,
      "maximum": 1
    },
    "overwrite": {
      "type": "boolean",
      "default": false
    },
    "openAfter": {
      "type": "boolean",
      "default": true
    },
    "port": {
      "type": "number",
      "default": 9877,
      "minimum": 1024,
      "maximum": 65515
    }
  },
  "required": [
    "characterName",
    "frontImage",
    "sideImage",
    "backImage",
    "threeQuarterImage",
    "outputBlend"
  ],
  "additionalProperties": false
}
```

</details>

#### `blender_character_loop_status`

Read a character-reference loop manifest and report exactly which stage and files are complete or missing.

Required: manifestPath

Parametros:

- `manifestPath`: string

<details>
<summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "manifestPath": {
      "type": "string"
    }
  },
  "required": [
    "manifestPath"
  ],
  "additionalProperties": false
}
```

</details>

### Tablet Whiteboard

#### `whiteboard_capture_pc_view`

Use this when the user asks you to look at, inspect, read, or review what is currently visible in TabletWhiteboard. Requests a fresh PNG from the connected PC browser at its exact current pan and zoom, then attaches the image to the tool result.

Required: ninguno

Parametros:

- `baseUrl`: string default="http://127.0.0.1:8787"
- `boardId`: string
- `timeoutMs`: number default=8000 min=1000 max=15000

<details>
<summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "baseUrl": {
      "type": "string",
      "default": "http://127.0.0.1:8787",
      "description": "Local or private-LAN TabletWhiteboard origin."
    },
    "boardId": {
      "type": "string",
      "description": "Optional board id. Defaults to the active board."
    },
    "timeoutMs": {
      "type": "number",
      "default": 8000,
      "minimum": 1000,
      "maximum": 15000
    }
  },
  "additionalProperties": false
}
```

</details>

#### `whiteboard_latest_capture`

Use this when the user asks to see the most recently saved TabletWhiteboard capture without forcing the PC to create a new one. Attaches that PNG to the tool result.

Required: ninguno

Parametros:

- `baseUrl`: string default="http://127.0.0.1:8787"
- `boardId`: string
- `timeoutMs`: number default=5000 min=1000 max=15000

<details>
<summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "baseUrl": {
      "type": "string",
      "default": "http://127.0.0.1:8787",
      "description": "Local or private-LAN TabletWhiteboard origin."
    },
    "boardId": {
      "type": "string",
      "description": "Optional board id. Omitting it returns the latest capture across boards."
    },
    "timeoutMs": {
      "type": "number",
      "default": 5000,
      "minimum": 1000,
      "maximum": 15000
    }
  },
  "additionalProperties": false
}
```

</details>

#### `whiteboard_capture_list`

Use this when the user asks which TabletWhiteboard screenshots are saved in the capture album. Returns bounded capture metadata without attaching every image.

Required: ninguno

Parametros:

- `baseUrl`: string default="http://127.0.0.1:8787"
- `boardId`: string
- `limit`: number default=20 min=1 max=100
- `timeoutMs`: number default=5000 min=1000 max=15000

<details>
<summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "baseUrl": {
      "type": "string",
      "default": "http://127.0.0.1:8787",
      "description": "Local or private-LAN TabletWhiteboard origin."
    },
    "boardId": {
      "type": "string",
      "description": "Optional board id used to filter the album."
    },
    "limit": {
      "type": "number",
      "default": 20,
      "minimum": 1,
      "maximum": 100
    },
    "timeoutMs": {
      "type": "number",
      "default": 5000,
      "minimum": 1000,
      "maximum": 15000
    }
  },
  "additionalProperties": false
}
```

</details>

### Git

#### `git_status`

Return short Git status for the current project.

Required: ninguno

Parametros:

- `cwd`: string

<details>
<summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "cwd": {
      "type": "string"
    }
  },
  "additionalProperties": false
}
```

</details>

#### `git_set_remote`

Add or update a GitHub HTTPS remote for the current project.

Required: ninguno

Parametros:

- `repoUrl`: string default="https://github.com/mauro3422/bridgeWEB-MCP.git"
- `remote`: string default="origin"
- `cwd`: string

<details>
<summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "repoUrl": {
      "type": "string",
      "default": "https://github.com/mauro3422/bridgeWEB-MCP.git"
    },
    "remote": {
      "type": "string",
      "default": "origin"
    },
    "cwd": {
      "type": "string"
    }
  },
  "additionalProperties": false
}
```

</details>

#### `git_commit_all`

Stage all changed repository paths and create a commit, but refuse the operation when any changed path is denied by the sensitive-path policy.

Required: message

Parametros:

- `message`: string
- `cwd`: string

<details>
<summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "message": {
      "type": "string"
    },
    "cwd": {
      "type": "string"
    }
  },
  "required": [
    "message"
  ],
  "additionalProperties": false
}
```

</details>

#### `git_push_current_branch`

Push the current Git branch to a remote using local credentials.

Required: ninguno

Parametros:

- `remote`: string default="origin"
- `branch`: string
- `cwd`: string

<details>
<summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "remote": {
      "type": "string",
      "default": "origin"
    },
    "branch": {
      "type": "string"
    },
    "cwd": {
      "type": "string"
    }
  },
  "additionalProperties": false
}
```

</details>

### Bridge ops

#### `tunnel_health`

Check tunnel-client local healthz and readyz endpoints using the configured tunnel admin URL by default.

Required: ninguno

Parametros:

- `baseUrl`: string default="http://127.0.0.1:8081"

<details>
<summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "baseUrl": {
      "type": "string",
      "default": "http://127.0.0.1:8081"
    }
  },
  "additionalProperties": false
}
```

</details>

#### `bridge_self_check`

Run typecheck, build, Git status, configured tunnel health, and terminal inventory.

Required: ninguno

Parametros:

- `cwd`: string

<details>
<summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "cwd": {
      "type": "string"
    }
  },
  "additionalProperties": false
}
```

</details>

#### `bridge_verify_all`

Run the full bridge verification workflow: doctor, check, build, HTTP smoke, regressions, docs/tools checks, watchdog/metrics sanity, tools/list sanity, and Git status.

Required: ninguno

Parametros:

- `cwd`: string
- `expectedServerVersion`: string default="0.6.6"
- `strictGit`: boolean default=false
- `timeoutMs`: number default=180000 min=30000 max=600000

<details>
<summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "cwd": {
      "type": "string"
    },
    "expectedServerVersion": {
      "type": "string",
      "default": "0.6.6"
    },
    "strictGit": {
      "type": "boolean",
      "default": false
    },
    "timeoutMs": {
      "type": "number",
      "default": 180000,
      "minimum": 30000,
      "maximum": 600000
    }
  },
  "additionalProperties": false
}
```

</details>

#### `bridge_request_restart`

Request a bridge restart by writing a restart-request file for the external watchdog. This tool does not restart or kill processes directly.

Required: reason

Parametros:

- `reason`: string
- `mode`: string enum=http|tunnel|full default="http"
- `cwd`: string

<details>
<summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "reason": {
      "type": "string"
    },
    "mode": {
      "type": "string",
      "enum": [
        "http",
        "tunnel",
        "full"
      ],
      "default": "http"
    },
    "cwd": {
      "type": "string"
    }
  },
  "required": [
    "reason"
  ],
  "additionalProperties": false
}
```

</details>

#### `bridge_restart_status`

Return pending restart-request and last restart-ack information for the bridge watchdog.

Required: ninguno

Parametros:

- `cwd`: string

<details>
<summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "cwd": {
      "type": "string"
    }
  },
  "additionalProperties": false
}
```

</details>

### Metricas / visualizaciones

#### `bridge_metrics_status`

Return metrics storage status and paths for bridge tool telemetry.

Required: ninguno

Parametros:

- sin parametros

<details>
<summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {},
  "additionalProperties": false
}
```

</details>

#### `bridge_metrics_summary`

Return aggregated bridge tool metrics from SQLite.

Required: ninguno

Parametros:

- `limit`: number default=50 min=1 max=200

<details>
<summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "limit": {
      "type": "number",
      "default": 50,
      "minimum": 1,
      "maximum": 200
    }
  },
  "additionalProperties": false
}
```

</details>

#### `bridge_metrics_recent`

Return recent bridge tool calls from SQLite.

Required: ninguno

Parametros:

- `limit`: number default=25 min=1 max=200

<details>
<summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "limit": {
      "type": "number",
      "default": 25,
      "minimum": 1,
      "maximum": 200
    }
  },
  "additionalProperties": false
}
```

</details>

#### `bridge_visualization_catalog`

Return available bridge visualization cards and chart kinds.

Required: ninguno

Parametros:

- sin parametros

<details>
<summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {},
  "additionalProperties": false
}
```

</details>

#### `bridge_visualize_metrics`

Use this when the user wants a visual chart/card for bridge metrics. Returns a chart spec compatible with ChatGPT chart rendering.

Required: ninguno

Parametros:

- `kind`: string enum=calls_by_tool|avg_duration_by_tool|errors_by_tool|activity_timeline|success_mix default="calls_by_tool"
- `limit`: number default=10 min=1 max=20

<details>
<summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "kind": {
      "type": "string",
      "enum": [
        "calls_by_tool",
        "avg_duration_by_tool",
        "errors_by_tool",
        "activity_timeline",
        "success_mix"
      ],
      "default": "calls_by_tool"
    },
    "limit": {
      "type": "number",
      "default": 10,
      "minimum": 1,
      "maximum": 20
    }
  },
  "additionalProperties": false
}
```

</details>

### Inteligencia de codigo

#### `analyze_code`

Analyze one text/code file. Uses TypeScript AST for TS/JS when available, otherwise regex fallback. Returns symbols, imports, exports, diagnostics, duplicates, and optional symbol references.

Required: path

Parametros:

- `path`: string
- `symbol`: string
- `maxSymbols`: number default=120 min=1 max=500
- `engine`: string enum=auto|regex|typescript|semantic default="auto"

<details>
<summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "path": {
      "type": "string"
    },
    "symbol": {
      "type": "string"
    },
    "maxSymbols": {
      "type": "number",
      "default": 120,
      "minimum": 1,
      "maximum": 500
    },
    "engine": {
      "type": "string",
      "enum": [
        "auto",
        "regex",
        "typescript",
        "semantic"
      ],
      "default": "auto"
    }
  },
  "required": [
    "path"
  ],
  "additionalProperties": false
}
```

</details>

#### `impact_analysis`

Find definitions and references for a symbol across a project. Uses TypeScript AST for TS/JS files when available, with regex fallback.

Required: name

Parametros:

- `name`: string
- `projectRoot`: string
- `filePattern`: string default="*.ts"
- `includeTests`: boolean default=false
- `maxFiles`: number default=500 min=1 max=2000
- `maxReferencesPerFile`: number default=20 min=1 max=100
- `engine`: string enum=auto|regex|typescript|semantic default="auto"

<details>
<summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "name": {
      "type": "string"
    },
    "projectRoot": {
      "type": "string"
    },
    "filePattern": {
      "type": "string",
      "default": "*.ts"
    },
    "includeTests": {
      "type": "boolean",
      "default": false
    },
    "maxFiles": {
      "type": "number",
      "default": 500,
      "minimum": 1,
      "maximum": 2000
    },
    "maxReferencesPerFile": {
      "type": "number",
      "default": 20,
      "minimum": 1,
      "maximum": 100
    },
    "engine": {
      "type": "string",
      "enum": [
        "auto",
        "regex",
        "typescript",
        "semantic"
      ],
      "default": "auto"
    }
  },
  "required": [
    "name"
  ],
  "additionalProperties": false
}
```

</details>

#### `find_duplicate_symbols`

Scan a project for duplicated symbol definitions by name and kind. Uses TypeScript AST for TS/JS files when available, with regex fallback.

Required: ninguno

Parametros:

- `projectRoot`: string
- `filePattern`: string default="*.ts"
- `includeTests`: boolean default=false
- `exportedOnly`: boolean default=false
- `minOccurrences`: number default=2 min=2 max=20
- `maxGroups`: number default=50 min=1 max=200
- `maxFiles`: number default=500 min=1 max=2000
- `engine`: string enum=auto|regex|typescript|semantic default="auto"

<details>
<summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "projectRoot": {
      "type": "string"
    },
    "filePattern": {
      "type": "string",
      "default": "*.ts"
    },
    "includeTests": {
      "type": "boolean",
      "default": false
    },
    "exportedOnly": {
      "type": "boolean",
      "default": false
    },
    "minOccurrences": {
      "type": "number",
      "default": 2,
      "minimum": 2,
      "maximum": 20
    },
    "maxGroups": {
      "type": "number",
      "default": 50,
      "minimum": 1,
      "maximum": 200
    },
    "maxFiles": {
      "type": "number",
      "default": 500,
      "minimum": 1,
      "maximum": 2000
    },
    "engine": {
      "type": "string",
      "enum": [
        "auto",
        "regex",
        "typescript",
        "semantic"
      ],
      "default": "auto"
    }
  },
  "additionalProperties": false
}
```

</details>

#### `import_graph`

Build an import graph for a TypeScript/JavaScript project, resolving relative imports and optionally showing external package imports.

Required: ninguno

Parametros:

- `projectRoot`: string
- `filePattern`: string default="*.ts"
- `includeTests`: boolean default=false
- `includeExternal`: boolean default=true
- `maxFiles`: number default=500 min=1 max=2000
- `maxCycles`: number default=20 min=0 max=100
- `resolutionEngine`: string enum=auto|relative|typescript default="auto"

<details>
<summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "projectRoot": {
      "type": "string"
    },
    "filePattern": {
      "type": "string",
      "default": "*.ts"
    },
    "includeTests": {
      "type": "boolean",
      "default": false
    },
    "includeExternal": {
      "type": "boolean",
      "default": true
    },
    "maxFiles": {
      "type": "number",
      "default": 500,
      "minimum": 1,
      "maximum": 2000
    },
    "maxCycles": {
      "type": "number",
      "default": 20,
      "minimum": 0,
      "maximum": 100
    },
    "resolutionEngine": {
      "type": "string",
      "enum": [
        "auto",
        "relative",
        "typescript"
      ],
      "default": "auto"
    }
  },
  "additionalProperties": false
}
```

</details>

#### `dependency_graph`

Return a dependency-oriented graph summary: internal edges, unresolved imports, cycles, most imported files, most importing files, and orphan files.

Required: ninguno

Parametros:

- `projectRoot`: string
- `filePattern`: string default="*.ts"
- `includeTests`: boolean default=false
- `maxFiles`: number default=500 min=1 max=2000
- `maxCycles`: number default=20 min=0 max=100
- `resolutionEngine`: string enum=auto|relative|typescript default="auto"

<details>
<summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "projectRoot": {
      "type": "string"
    },
    "filePattern": {
      "type": "string",
      "default": "*.ts"
    },
    "includeTests": {
      "type": "boolean",
      "default": false
    },
    "maxFiles": {
      "type": "number",
      "default": 500,
      "minimum": 1,
      "maximum": 2000
    },
    "maxCycles": {
      "type": "number",
      "default": 20,
      "minimum": 0,
      "maximum": 100
    },
    "resolutionEngine": {
      "type": "string",
      "enum": [
        "auto",
        "relative",
        "typescript"
      ],
      "default": "auto"
    }
  },
  "additionalProperties": false
}
```

</details>

#### `call_graph`

Build a TypeScript semantic call graph showing which project functions/methods call each other, plus external/unresolved calls.

Required: ninguno

Parametros:

- `projectRoot`: string
- `includeTests`: boolean default=false
- `includeExternal`: boolean default=false
- `maxFiles`: number default=500 min=1 max=2000
- `maxCycles`: number default=20 min=0 max=100

<details>
<summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "projectRoot": {
      "type": "string"
    },
    "includeTests": {
      "type": "boolean",
      "default": false
    },
    "includeExternal": {
      "type": "boolean",
      "default": false
    },
    "maxFiles": {
      "type": "number",
      "default": 500,
      "minimum": 1,
      "maximum": 2000
    },
    "maxCycles": {
      "type": "number",
      "default": 20,
      "minimum": 0,
      "maximum": 100
    }
  },
  "additionalProperties": false
}
```

</details>

#### `find_dead_code`

Find initial dead-code candidates using TypeScript AST references. Conservative: local symbols only by default; exported symbols are optional and lower-confidence.

Required: ninguno

Parametros:

- `projectRoot`: string
- `filePattern`: string default="*.ts"
- `includeTests`: boolean default=false
- `includeExported`: boolean default=false
- `maxFiles`: number default=500 min=1 max=2000
- `maxCandidates`: number default=100 min=1 max=500
- `engine`: string enum=auto|typescript|semantic default="semantic"

<details>
<summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "projectRoot": {
      "type": "string"
    },
    "filePattern": {
      "type": "string",
      "default": "*.ts"
    },
    "includeTests": {
      "type": "boolean",
      "default": false
    },
    "includeExported": {
      "type": "boolean",
      "default": false
    },
    "maxFiles": {
      "type": "number",
      "default": 500,
      "minimum": 1,
      "maximum": 2000
    },
    "maxCandidates": {
      "type": "number",
      "default": 100,
      "minimum": 1,
      "maximum": 500
    },
    "engine": {
      "type": "string",
      "enum": [
        "auto",
        "typescript",
        "semantic"
      ],
      "default": "semantic"
    }
  },
  "additionalProperties": false
}
```

</details>

### Otras tools

#### `skill_catalog`

List the unified skill catalog available to Mauro's workflow: local/system/plugin Codex SKILL.md files plus the Roblox-authored skills exposed by Roblox Studio MCP. Use when the user asks what skills exist, when resuming a specialized workflow, or when you need to discover whether a reusable procedure applies.

```json
{
  "type": "object",
  "properties": {
    "query": {
      "type": "string",
      "description": "Optional name/description filter."
    },
    "sources": {
      "type": "array",
      "items": {
        "type": "string",
        "enum": [
          "codex-local",
          "codex-system",
          "codex-plugin",
          "roblox"
        ]
      }
    },
    "maxResults": {
      "type": "number",
      "default": 100,
      "minimum": 1,
      "maximum": 600
    }
  },
  "additionalProperties": false
}
```

#### `skill_recommend`

Recommend relevant Codex and Roblox MCP skills for a task. Use before substantial specialized work when a skill may encode a safer or more complete loop, especially for Roblox Studio, Blender, asset review, animation, QA, or repeatable maintenance workflows.

```json
{
  "type": "object",
  "properties": {
    "task": {
      "type": "string",
      "description": "Current user request or intended workflow."
    },
    "sources": {
      "type": "array",
      "items": {
        "type": "string",
        "enum": [
          "codex-local",
          "codex-system",
          "codex-plugin",
          "roblox"
        ]
      }
    },
    "maxResults": {
      "type": "number",
      "default": 8,
      "minimum": 1,
      "maximum": 30
    }
  },
  "required": [
    "task"
  ],
  "additionalProperties": false
}
```

#### `skill_route_audit`

Audit the live skill catalog against the Git-tracked routing contract. Detect unconfigured owned skills, stale config entries, missing dependencies/workflow references, cycles, unreadable or oversized skills, duplicates, and inferred routing that needs review. Run after adding, renaming, deleting, or materially changing a skill, and as a required verification gate before committing routing changes.

```json
{
  "type": "object",
  "properties": {
    "sources": {
      "type": "array",
      "items": {
        "type": "string",
        "enum": [
          "codex-local",
          "codex-system",
          "codex-plugin",
          "roblox"
        ]
      }
    }
  },
  "additionalProperties": false
}
```

#### `skill_route_plan`

Plan skill activation before substantial specialized work. The agent should first infer a compact structured intent from the user's request, even when the wording is incomplete, then call this tool. It deterministically applies routing metadata, dependencies, exclusions, workflow phases, source precedence, and completed-phase coverage. It does not expose or require chain-of-thought.

```json
{
  "type": "object",
  "properties": {
    "task": {
      "type": "string",
      "description": "Current user message or concise task statement."
    },
    "context": {
      "type": "string",
      "maxLength": 4000,
      "description": "Bounded resolved context from the recent relevant conversation. For multi-turn specialized work, normally pass a 500-2000 character summary covering the accepted goal, constraints, completed work/current phase, and unresolved references, even when the current message is not an obvious acknowledgment. Omit only for a genuinely standalone first turn. Do not send hidden chain-of-thought, irrelevant history, or a full transcript."
    },
    "intent": {
      "type": "object",
      "description": "Compact semantic classification inferred by the agent from the user's request. This is not chain-of-thought; provide only the structured outcome.",
      "properties": {
        "summary": {
          "type": "string",
          "maxLength": 600
        },
        "domains": {
          "type": "array",
          "minItems": 1,
          "maxItems": 8,
          "items": {
            "type": "string",
            "enum": [
              "roblox",
              "blender",
              "figma",
              "github",
              "git",
              "filesystem",
              "google-workspace",
              "openai-development",
              "artifacts",
              "browser",
              "coding",
              "opencode",
              "agent-orchestration",
              "skill-system",
              "other"
            ]
          }
        },
        "actions": {
          "type": "array",
          "minItems": 1,
          "maxItems": 12,
          "items": {
            "type": "string",
            "enum": [
              "discover",
              "design",
              "create",
              "edit",
              "move",
              "review",
              "verify",
              "test",
              "debug",
              "optimize",
              "save",
              "recover",
              "version",
              "publish",
              "coordinate",
              "maintain",
              "document",
              "analyze"
            ]
          }
        },
        "artifacts": {
          "type": "array",
          "maxItems": 12,
          "items": {
            "type": "string",
            "enum": [
              "code",
              "game",
              "project",
              "repository",
              "place-file",
              "backup",
              "ui",
              "animation",
              "model-3d",
              "asset",
              "document",
              "spreadsheet",
              "presentation",
              "diagram",
              "website",
              "skill",
              "mcp",
              "placement-system",
              "network-system",
              "resource-system"
            ]
          },
          "default": []
        },
        "needs": {
          "type": "array",
          "maxItems": 12,
          "items": {
            "type": "string",
            "enum": [
              "official-docs",
              "safe-editing",
              "unit-tests",
              "playtest",
              "visual-qa",
              "device-testing",
              "performance",
              "scene-analysis",
              "backup",
              "integrity-verification",
              "version-control",
              "history-recovery",
              "cross-agent",
              "human-approval"
            ]
          },
          "default": []
        },
        "risk": {
          "type": "string",
          "enum": [
            "read-only",
            "write",
            "destructive",
            "external-side-effect"
          ],
          "default": "read-only"
        },
        "ambiguity": {
          "type": "string",
          "enum": [
            "low",
            "medium",
            "high"
          ],
          "default": "low"
        }
      },
      "required": [
        "domains",
        "actions"
      ],
      "additionalProperties": false
    },
    "stage": {
      "type": "string",
      "enum": [
        "start",
        "implement",
        "verify",
        "persist",
        "close",
        "resume"
      ],
      "default": "start"
    },
    "completedPhases": {
      "type": "array",
      "items": {
        "type": "string",
        "enum": [
          "discovery",
          "safety",
          "implementation",
          "verification",
          "persistence",
          "maintenance"
        ]
      },
      "default": []
    },
    "sources": {
      "type": "array",
      "items": {
        "type": "string",
        "enum": [
          "codex-local",
          "codex-system",
          "codex-plugin",
          "roblox"
        ]
      }
    },
    "maxSkills": {
      "type": "number",
      "default": 8,
      "minimum": 1,
      "maximum": 16
    }
  },
  "required": [
    "task"
  ],
  "additionalProperties": false
}
```

#### `skill_bootstrap`

Load the current phase of a structured skill route. Use at start, implementation, verification, persistence, close, or resume. It rescans the canonical Codex skills and live Roblox catalog, plans activation deterministically, and loads only active-phase skills; deferred skills remain in the returned plan to avoid context bloat. Provide structured intent whenever possible; lexical matching is a marked fallback only.

```json
{
  "type": "object",
  "properties": {
    "task": {
      "type": "string",
      "description": "Current user message or concise task statement."
    },
    "context": {
      "type": "string",
      "maxLength": 4000,
      "description": "Bounded resolved context from the recent relevant conversation. For multi-turn specialized work, normally pass a 500-2000 character summary covering the accepted goal, constraints, completed work/current phase, and unresolved references, even when the current message is not an obvious acknowledgment. Omit only for a genuinely standalone first turn. Do not send hidden chain-of-thought, irrelevant history, or a full transcript."
    },
    "intent": {
      "type": "object",
      "description": "Compact semantic classification inferred by the agent from the user's request. This is not chain-of-thought; provide only the structured outcome.",
      "properties": {
        "summary": {
          "type": "string",
          "maxLength": 600
        },
        "domains": {
          "type": "array",
          "minItems": 1,
          "maxItems": 8,
          "items": {
            "type": "string",
            "enum": [
              "roblox",
              "blender",
              "figma",
              "github",
              "git",
              "filesystem",
              "google-workspace",
              "openai-development",
              "artifacts",
              "browser",
              "coding",
              "opencode",
              "agent-orchestration",
              "skill-system",
              "other"
            ]
          }
        },
        "actions": {
          "type": "array",
          "minItems": 1,
          "maxItems": 12,
          "items": {
            "type": "string",
            "enum": [
              "discover",
              "design",
              "create",
              "edit",
              "move",
              "review",
              "verify",
              "test",
              "debug",
              "optimize",
              "save",
              "recover",
              "version",
              "publish",
              "coordinate",
              "maintain",
              "document",
              "analyze"
            ]
          }
        },
        "artifacts": {
          "type": "array",
          "maxItems": 12,
          "items": {
            "type": "string",
            "enum": [
              "code",
              "game",
              "project",
              "repository",
              "place-file",
              "backup",
              "ui",
              "animation",
              "model-3d",
              "asset",
              "document",
              "spreadsheet",
              "presentation",
              "diagram",
              "website",
              "skill",
              "mcp",
              "placement-system",
              "network-system",
              "resource-system"
            ]
          },
          "default": []
        },
        "needs": {
          "type": "array",
          "maxItems": 12,
          "items": {
            "type": "string",
            "enum": [
              "official-docs",
              "safe-editing",
              "unit-tests",
              "playtest",
              "visual-qa",
              "device-testing",
              "performance",
              "scene-analysis",
              "backup",
              "integrity-verification",
              "version-control",
              "history-recovery",
              "cross-agent",
              "human-approval"
            ]
          },
          "default": []
        },
        "risk": {
          "type": "string",
          "enum": [
            "read-only",
            "write",
            "destructive",
            "external-side-effect"
          ],
          "default": "read-only"
        },
        "ambiguity": {
          "type": "string",
          "enum": [
            "low",
            "medium",
            "high"
          ],
          "default": "low"
        }
      },
      "required": [
        "domains",
        "actions"
      ],
      "additionalProperties": false
    },
    "stage": {
      "type": "string",
      "enum": [
        "start",
        "implement",
        "verify",
        "persist",
        "close",
        "resume"
      ],
      "default": "start"
    },
    "completedPhases": {
      "type": "array",
      "items": {
        "type": "string",
        "enum": [
          "discovery",
          "safety",
          "implementation",
          "verification",
          "persistence",
          "maintenance"
        ]
      },
      "default": []
    },
    "sources": {
      "type": "array",
      "items": {
        "type": "string",
        "enum": [
          "codex-local",
          "codex-system",
          "codex-plugin",
          "roblox"
        ]
      }
    },
    "maxSkills": {
      "type": "number",
      "default": 8,
      "minimum": 1,
      "maximum": 16
    }
  },
  "required": [
    "task"
  ],
  "additionalProperties": false
}
```

#### `skill_load`

Load one skill as active guidance. For Codex skills this reads the exact SKILL.md; for Roblox-authored skills this invokes the Roblox Studio MCP skill tool. Use only after naming or recommending the skill, and load it before writing code or taking the covered action.

```json
{
  "type": "object",
  "properties": {
    "name": {
      "type": "string"
    },
    "source": {
      "type": "string",
      "enum": [
        "auto",
        "codex",
        "roblox"
      ],
      "default": "auto"
    }
  },
  "required": [
    "name"
  ],
  "additionalProperties": false
}
```

#### `roblox_mcp_status`

Check the persistent Bridge-to-Roblox Studio MCP connection, server version, available Studio instances, current Studio state, and tool count. Use before Roblox edits or when the MCP appears disconnected or stale.

```json
{
  "type": "object",
  "properties": {},
  "additionalProperties": false
}
```

#### `roblox_mcp_tool_list`

List the live tools exposed by Roblox Studio MCP, including newly added tools and their schemas. Use when a Roblox capability may exist but is not represented by a dedicated Bridge tool.

```json
{
  "type": "object",
  "properties": {
    "query": {
      "type": "string",
      "description": "Optional name/description filter."
    },
    "includeSchemas": {
      "type": "boolean",
      "default": true
    }
  },
  "additionalProperties": false
}
```

#### `roblox_mcp_query`

Call a live Roblox Studio MCP tool only when its remote annotation marks it read-only. Use roblox_mcp_tool_list first to inspect the exact schema. This provides forward-compatible access to current and future Roblox query/inspection tools.

```json
{
  "type": "object",
  "properties": {
    "toolName": {
      "type": "string"
    },
    "arguments": {
      "type": "object",
      "additionalProperties": true,
      "default": {}
    }
  },
  "required": [
    "toolName"
  ],
  "additionalProperties": false
}
```

#### `roblox_mcp_action`

Call a live non-read-only Roblox Studio MCP tool through the Bridge. Before using it, list Studios, verify the active instance and Edit/Play state, inspect the remote schema, and require confirmToolName to exactly match toolName. This is the forward-compatible mutation path for current and future Roblox tools.

```json
{
  "type": "object",
  "properties": {
    "toolName": {
      "type": "string"
    },
    "confirmToolName": {
      "type": "string"
    },
    "arguments": {
      "type": "object",
      "additionalProperties": true,
      "default": {}
    }
  },
  "required": [
    "toolName",
    "confirmToolName"
  ],
  "additionalProperties": false
}
```

#### `roblox_place_save`

Save one open local Roblox Studio .rbxl/.rbxlx place through a narrow verified Ctrl+S operation. This is not generic computer control: it requires an exact place path confirmation, matches the connected Studio and window title, forces Edit mode, focuses only that Roblox Studio window, sends only Ctrl+S, and verifies the place file timestamp/hash. Use after persistent Studio edits when disk save must be proven separately from Edit DataModel and Play verification.

```json
{
  "type": "object",
  "properties": {
    "placePath": {
      "type": "string",
      "description": "Exact local .rbxl or .rbxlx path currently open in Studio."
    },
    "confirmPlacePath": {
      "type": "string",
      "description": "Must resolve to the exact same path as placePath."
    },
    "stopPlay": {
      "type": "boolean",
      "default": true,
      "description": "Stop Play and return Studio to Edit before saving."
    },
    "verifyTimeoutMs": {
      "type": "number",
      "default": 5000,
      "minimum": 1000,
      "maximum": 20000
    }
  },
  "required": [
    "placePath",
    "confirmPlacePath"
  ],
  "additionalProperties": false
}
```

#### `work_once`

Alias of run_command for one short project action.

```json
{
  "type": "object",
  "properties": {
    "command": {
      "type": "string"
    },
    "cwd": {
      "type": "string"
    },
    "timeoutMs": {
      "type": "number",
      "default": 30000
    }
  },
  "required": [
    "command"
  ],
  "additionalProperties": false
}
```

#### `work_begin`

Alias of terminal_start for long-running project work.

```json
{
  "type": "object",
  "properties": {
    "command": {
      "type": "string"
    },
    "cwd": {
      "type": "string"
    },
    "name": {
      "type": "string"
    },
    "logFile": {
      "type": "string"
    },
    "timeoutMs": {
      "type": "number",
      "minimum": 1000,
      "maximum": 86400000
    },
    "cleanupAfterMs": {
      "type": "number",
      "default": 600000,
      "minimum": 0,
      "maximum": 86400000
    }
  },
  "additionalProperties": false
}
```

#### `work_peek`

Alias of terminal_read for inspecting project work output.

```json
{
  "type": "object",
  "properties": {
    "sessionId": {
      "type": "string"
    },
    "maxChars": {
      "type": "number",
      "default": 20000
    }
  },
  "required": [
    "sessionId"
  ],
  "additionalProperties": false
}
```

#### `work_show`

Alias of terminal_list for listing project work.

```json
{
  "type": "object",
  "properties": {},
  "additionalProperties": false
}
```

#### `work_feed`

Alias of terminal_write for sending input to project work.

```json
{
  "type": "object",
  "properties": {
    "sessionId": {
      "type": "string"
    },
    "input": {
      "type": "string"
    }
  },
  "required": [
    "sessionId",
    "input"
  ],
  "additionalProperties": false
}
```

#### `work_finish`

Alias of terminal_stop for stopping project work.

```json
{
  "type": "object",
  "properties": {
    "sessionId": {
      "type": "string"
    }
  },
  "required": [
    "sessionId"
  ],
  "additionalProperties": false
}
```

#### `git_diff`

Return a bounded Git diff while omitting and reporting paths denied by the sensitive-path policy; optionally staged, against a ref, or limited to repository paths.

```json
{
  "type": "object",
  "properties": {
    "cwd": {
      "type": "string"
    },
    "staged": {
      "type": "boolean",
      "default": false
    },
    "ref": {
      "type": "string"
    },
    "paths": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "maxItems": 50
    },
    "maxChars": {
      "type": "number",
      "default": 50000,
      "minimum": 1000,
      "maximum": 200000
    }
  },
  "additionalProperties": false
}
```

#### `git_log`

Return recent Git commits with hash, timestamp, author, and subject.

```json
{
  "type": "object",
  "properties": {
    "cwd": {
      "type": "string"
    },
    "limit": {
      "type": "number",
      "default": 20,
      "minimum": 1,
      "maximum": 200
    },
    "ref": {
      "type": "string"
    },
    "maxChars": {
      "type": "number",
      "default": 50000,
      "minimum": 1000,
      "maximum": 200000
    }
  },
  "additionalProperties": false
}
```

#### `git_show_commit`

Inspect one Git commit with metadata and either a summary or bounded patch, omitting and reporting sensitive denied paths.

```json
{
  "type": "object",
  "properties": {
    "cwd": {
      "type": "string"
    },
    "ref": {
      "type": "string",
      "default": "HEAD"
    },
    "includePatch": {
      "type": "boolean",
      "default": false
    },
    "maxChars": {
      "type": "number",
      "default": 50000,
      "minimum": 1000,
      "maximum": 200000
    }
  },
  "additionalProperties": false
}
```

#### `git_compare_branches`

Compare two Git refs using sensitive-path-filtered three-dot diff statistics and commits unique to the head ref.

```json
{
  "type": "object",
  "properties": {
    "cwd": {
      "type": "string"
    },
    "base": {
      "type": "string"
    },
    "head": {
      "type": "string"
    },
    "maxChars": {
      "type": "number",
      "default": 50000,
      "minimum": 1000,
      "maximum": 200000
    }
  },
  "required": [
    "base",
    "head"
  ],
  "additionalProperties": false
}
```

#### `git_create_branch`

Create a validated Git branch and optionally switch to it.

```json
{
  "type": "object",
  "properties": {
    "cwd": {
      "type": "string"
    },
    "name": {
      "type": "string"
    },
    "startPoint": {
      "type": "string"
    },
    "checkout": {
      "type": "boolean",
      "default": true
    }
  },
  "required": [
    "name"
  ],
  "additionalProperties": false
}
```

#### `git_restore_file`

Restore one repository file from a validated Git source into the index and/or working tree.

```json
{
  "type": "object",
  "properties": {
    "cwd": {
      "type": "string"
    },
    "path": {
      "type": "string"
    },
    "source": {
      "type": "string",
      "default": "HEAD"
    },
    "staged": {
      "type": "boolean",
      "default": false
    },
    "worktree": {
      "type": "boolean",
      "default": true
    }
  },
  "required": [
    "path"
  ],
  "additionalProperties": false
}
```

#### `path_policy_status`

Show active allowed roots, denied paths, denied sensitive filenames, and path-policy guidance.

```json
{
  "type": "object",
  "properties": {},
  "additionalProperties": false
}
```

#### `project_profile`

Detect a project's languages, frameworks, package manager, scripts, commands, important files, Git state, and saved Bridge profile.

```json
{
  "type": "object",
  "properties": {
    "projectRoot": {
      "type": "string"
    }
  },
  "additionalProperties": false
}
```

#### `project_profile_save`

Save a .bridge-project.json profile containing detected project commands plus explicit overrides.

```json
{
  "type": "object",
  "properties": {
    "projectRoot": {
      "type": "string"
    },
    "overrides": {
      "type": "object",
      "additionalProperties": true,
      "default": {}
    }
  },
  "additionalProperties": false
}
```

#### `workspace_snapshot`

Create a bounded content-addressed workspace snapshot outside the project, excluding generated/noisy directories and sensitive denied paths.

```json
{
  "type": "object",
  "properties": {
    "projectRoot": {
      "type": "string"
    },
    "label": {
      "type": "string"
    },
    "maxFiles": {
      "type": "number",
      "default": 1000,
      "minimum": 1,
      "maximum": 5000
    },
    "maxFileBytes": {
      "type": "number",
      "default": 2097152,
      "minimum": 1024,
      "maximum": 10485760
    },
    "maxTotalBytes": {
      "type": "number",
      "default": 26214400,
      "minimum": 1024,
      "maximum": 104857600
    }
  },
  "additionalProperties": false
}
```

#### `workspace_diff`

Compare the current workspace with a saved snapshot by file hashes.

```json
{
  "type": "object",
  "properties": {
    "snapshotId": {
      "type": "string"
    },
    "projectRoot": {
      "type": "string"
    },
    "maxChanges": {
      "type": "number",
      "default": 200,
      "minimum": 1,
      "maximum": 2000
    }
  },
  "required": [
    "snapshotId"
  ],
  "additionalProperties": false
}
```

#### `workspace_rollback`

Restore files from a complete snapshot after preflight hash/path checks. Requires an exact repeated snapshot id; optional removal affects only newly added files from a complete current scan.

```json
{
  "type": "object",
  "properties": {
    "snapshotId": {
      "type": "string"
    },
    "confirmSnapshotId": {
      "type": "string"
    },
    "projectRoot": {
      "type": "string"
    },
    "removeAddedFiles": {
      "type": "boolean",
      "default": false
    }
  },
  "required": [
    "snapshotId",
    "confirmSnapshotId"
  ],
  "additionalProperties": false
}
```

#### `workspace_snapshot_list`

List recent workspace snapshots with roots, timestamps, sizes, and Git metadata.

```json
{
  "type": "object",
  "properties": {
    "limit": {
      "type": "number",
      "default": 20,
      "minimum": 1,
      "maximum": 200
    }
  },
  "additionalProperties": false
}
```

#### `cache_status`

Return persistent analysis-cache location, entry count, size, age, and active limits.

```json
{
  "type": "object",
  "properties": {},
  "additionalProperties": false
}
```

#### `cache_prune`

Prune persistent analysis-cache entries by TTL, total bytes, and entry count; supports dry-run.

```json
{
  "type": "object",
  "properties": {
    "ttlMs": {
      "type": "number",
      "minimum": 0
    },
    "maxBytes": {
      "type": "number",
      "minimum": 0
    },
    "maxEntries": {
      "type": "number",
      "minimum": 0
    },
    "dryRun": {
      "type": "boolean",
      "default": true
    }
  },
  "additionalProperties": false
}
```

#### `bridge_health`

Compact read-only bridge health query for tunnel, restart status, runtime tool catalog, or all lightweight checks.

```json
{
  "type": "object",
  "properties": {
    "check": {
      "type": "string",
      "enum": [
        "all",
        "tunnel",
        "restart",
        "catalog"
      ],
      "default": "all"
    },
    "cwd": {
      "type": "string"
    }
  },
  "additionalProperties": false
}
```

#### `bridge_metrics_query`

Compact read-only bridge metrics query. Use kind=status, summary, recent, visualization_catalog, or visualize.

```json
{
  "type": "object",
  "properties": {
    "kind": {
      "type": "string",
      "enum": [
        "status",
        "summary",
        "recent",
        "visualization_catalog",
        "visualize"
      ],
      "default": "summary"
    },
    "limit": {
      "type": "number",
      "default": 50,
      "minimum": 1,
      "maximum": 200
    },
    "chartKind": {
      "type": "string",
      "enum": [
        "calls_by_tool",
        "avg_duration_by_tool",
        "errors_by_tool",
        "activity_timeline",
        "success_mix"
      ],
      "default": "calls_by_tool"
    }
  },
  "additionalProperties": false
}
```

#### `python_validate`

Validate Python syntax using the local Python interpreter. Accepts one file or a project scan and returns py_compile diagnostics.

```json
{
  "type": "object",
  "properties": {
    "path": {
      "type": "string"
    },
    "projectRoot": {
      "type": "string"
    },
    "filePattern": {
      "type": "string",
      "default": "*.py"
    },
    "includeTests": {
      "type": "boolean",
      "default": false
    },
    "maxFiles": {
      "type": "number",
      "default": 300,
      "minimum": 1,
      "maximum": 1000
    },
    "timeoutMs": {
      "type": "number",
      "default": 30000,
      "minimum": 1000,
      "maximum": 120000
    }
  },
  "additionalProperties": false
}
```

#### `python_symbols`

Analyze Python symbols in one file or project scan. Returns imports, function/class definitions, and simple assignments.

```json
{
  "type": "object",
  "properties": {
    "path": {
      "type": "string"
    },
    "projectRoot": {
      "type": "string"
    },
    "filePattern": {
      "type": "string",
      "default": "*.py"
    },
    "includeTests": {
      "type": "boolean",
      "default": false
    },
    "maxFiles": {
      "type": "number",
      "default": 300,
      "minimum": 1,
      "maximum": 1000
    },
    "maxSymbols": {
      "type": "number",
      "default": 300,
      "minimum": 1,
      "maximum": 2000
    }
  },
  "additionalProperties": false
}
```

#### `python_impact_analysis`

Find Python definitions and name references for a symbol across a project using conservative static scanning.

```json
{
  "type": "object",
  "properties": {
    "name": {
      "type": "string"
    },
    "projectRoot": {
      "type": "string"
    },
    "filePattern": {
      "type": "string",
      "default": "*.py"
    },
    "includeTests": {
      "type": "boolean",
      "default": false
    },
    "maxFiles": {
      "type": "number",
      "default": 500,
      "minimum": 1,
      "maximum": 2000
    },
    "maxReferencesPerFile": {
      "type": "number",
      "default": 20,
      "minimum": 1,
      "maximum": 100
    }
  },
  "required": [
    "name"
  ],
  "additionalProperties": false
}
```

#### `python_import_graph`

Build a conservative Python import graph from import/from statements, resolving internal absolute and relative imports where possible.

```json
{
  "type": "object",
  "properties": {
    "projectRoot": {
      "type": "string"
    },
    "filePattern": {
      "type": "string",
      "default": "*.py"
    },
    "includeTests": {
      "type": "boolean",
      "default": false
    },
    "includeExternal": {
      "type": "boolean",
      "default": true
    },
    "maxFiles": {
      "type": "number",
      "default": 500,
      "minimum": 1,
      "maximum": 2000
    },
    "maxCycles": {
      "type": "number",
      "default": 20,
      "minimum": 0,
      "maximum": 100
    }
  },
  "additionalProperties": false
}
```

#### `python_call_graph`

Build a Python call graph using the stdlib ast parser. Reports internal calls, unresolved/external calls, cycles, hot callers, and hot callees without importing project code.

```json
{
  "type": "object",
  "properties": {
    "projectRoot": {
      "type": "string"
    },
    "filePattern": {
      "type": "string",
      "default": "*.py"
    },
    "includeTests": {
      "type": "boolean",
      "default": false
    },
    "includeExternal": {
      "type": "boolean",
      "default": false
    },
    "maxFiles": {
      "type": "number",
      "default": 500,
      "minimum": 1,
      "maximum": 2000
    },
    "maxCycles": {
      "type": "number",
      "default": 20,
      "minimum": 0,
      "maximum": 100
    }
  },
  "additionalProperties": false
}
```

#### `python_test_plan`

Suggest a small Python test plan for changed files or a symbol. Designed for focused pytest/testmon runs.

```json
{
  "type": "object",
  "properties": {
    "projectRoot": {
      "type": "string"
    },
    "changedFiles": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "default": []
    },
    "symbol": {
      "type": "string"
    },
    "filePattern": {
      "type": "string",
      "default": "*.py"
    },
    "maxFiles": {
      "type": "number",
      "default": 1200,
      "minimum": 1,
      "maximum": 3000
    },
    "maxTests": {
      "type": "number",
      "default": 30,
      "minimum": 1,
      "maximum": 200
    }
  },
  "additionalProperties": false
}
```

#### `pytest_testmon`

Run focused pytest using pytest-testmon when available. Defaults to python -m pytest --testmon and bounded output.

```json
{
  "type": "object",
  "properties": {
    "projectRoot": {
      "type": "string"
    },
    "paths": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "default": []
    },
    "extraArgs": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "default": []
    },
    "timeoutMs": {
      "type": "number",
      "default": 120000,
      "minimum": 1000,
      "maximum": 600000
    }
  },
  "additionalProperties": false
}
```

#### `python_dead_code`

Find conservative Python dead-code candidates by scanning function/class definitions and project-wide name references. Dynamic usage can produce false positives.

```json
{
  "type": "object",
  "properties": {
    "projectRoot": {
      "type": "string"
    },
    "filePattern": {
      "type": "string",
      "default": "*.py"
    },
    "includeTests": {
      "type": "boolean",
      "default": false
    },
    "includeExported": {
      "type": "boolean",
      "default": false
    },
    "includeDunder": {
      "type": "boolean",
      "default": false
    },
    "maxFiles": {
      "type": "number",
      "default": 500,
      "minimum": 1,
      "maximum": 2000
    },
    "maxCandidates": {
      "type": "number",
      "default": 100,
      "minimum": 1,
      "maximum": 500
    }
  },
  "additionalProperties": false
}
```

#### `bridge_tool_query`

Use this read-only fallback when a runtime Bridge tool exists but its dedicated schema is missing from the current connector catalog. Delegates only to tools classified read-only.

```json
{
  "type": "object",
  "properties": {
    "toolName": {
      "type": "string",
      "description": "Exact runtime tool name to invoke."
    },
    "arguments": {
      "type": "object",
      "description": "Arguments for the delegated tool.",
      "additionalProperties": true,
      "default": {}
    }
  },
  "required": [
    "toolName"
  ],
  "additionalProperties": false
}
```

#### `bridge_tool_action`

Use this explicit destructive fallback when a runtime Bridge tool exists but its dedicated schema is missing from the current connector catalog. Delegates only to tools classified destructive and requires exact target-name confirmation.

```json
{
  "type": "object",
  "properties": {
    "toolName": {
      "type": "string",
      "description": "Exact runtime tool name to invoke."
    },
    "confirmToolName": {
      "type": "string",
      "description": "Must exactly match toolName to confirm the delegated destructive action."
    },
    "arguments": {
      "type": "object",
      "description": "Arguments for the delegated tool.",
      "additionalProperties": true,
      "default": {}
    }
  },
  "required": [
    "toolName",
    "confirmToolName"
  ],
  "additionalProperties": false
}
```

## Notas de seguridad

- No leer ni imprimir secretos, tokens ni archivos `.env`.
- No commitear `node_modules`, `dist`, logs, bases SQLite, sandbox ni binarios del tunnel-client.
- Para restart, usar `bridge_request_restart`; no matar procesos directamente desde el MCP activo.
- `run_command` es fallback, no el camino principal para editar o diagnosticar cuando hay tools especificas.
